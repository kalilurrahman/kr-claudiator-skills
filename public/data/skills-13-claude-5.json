{
  "category": "Claude 5 Mythos & Fable",
  "categorySlug": "13-claude-5",
  "skills": [
    {
      "id": "13-claude-5/long-context-1m-tokens",
      "name": "long-context-1m-tokens",
      "displayName": "Long-Context Reasoning (1M+ tokens)",
      "category": "Claude 5 Mythos & Fable",
      "categorySlug": "13-claude-5",
      "description": "Design prompts that exploit Claude 5 Mythos/Fable's 1M+ token window for whole-codebase reasoning, multi-doc synthesis, and cross-file refactors without lossy summarisation.",
      "argumentHint": "[corpus size, retrieval strategy, chunking preference, output format]",
      "allowedTools": "Read, Write",
      "content": "---\nname: long-context-1m-tokens\ndescription: Design prompts that exploit Claude 5 Mythos/Fable's 1M+ token window for whole-codebase reasoning, multi-doc synthesis, and cross-file refactors without lossy summarisation.\nargument-hint: [corpus size, retrieval strategy, chunking preference, output format]\nallowed-tools: Read, Write\nmodel: claude-5-mythos | claude-5-fable\ncapability: Long-Context Reasoning\n---\n\n# Long-Context Reasoning (1M+ tokens)\n\n> Claude 5 Mythos + Fable skill — Long-Context Reasoning. Optimised for Claude 5 Mythos (reasoning) and Claude 5 Fable (action / tool use), and backward-compatible with Claude Sonnet 4.5 and Opus 4.\n\n## TL;DR\n\n1M+ token windows let Claude Mythos reason across an entire repo, contract binder, or research corpus in a single pass. This skill teaches you the loading order, cache-friendly structure, and citation discipline that keeps recall high and hallucinations low.\n\n## When to use this skill\n\n- You are working with Claude 5 Mythos or Fable (or a downstream vendor exposing them).\n- The task benefits from **long-context reasoning** — do not use for tasks a smaller model handles cheaply.\n- You need a repeatable, reviewable prompt — not a one-off chat.\n- Output has to survive a code review, an audit, or a partner sign-off.\n\n## When NOT to use this skill\n\n- Simple lookups a smaller model (Haiku 4 / Sonnet 4.5) can serve at 10× lower cost.\n- Real-time UX where 1M-token loads would break the latency budget.\n- Tasks where a deterministic script would do — do not model what you can compute.\n\n## Inputs (fill these in)\n\n| Field | Required | Notes |\n|---|---|---|\n| `context_source` | ✅ | Repo path, doc set, ticket, screenshot, or MCP resource id |\n| `goal` | ✅ | The single sentence definition of done |\n| `audience` | ✅ | Reviewer, end-user, downstream agent, or auditor |\n| `constraints` | ✅ | Token budget, step budget, permitted tools, forbidden ops |\n| `output_shape` | ✅ | Free text / markdown / JSON schema / patch / PR |\n| `citations` | ⛔ optional | Required for research, legal, medical, financial outputs |\n| `escalation` | ⛔ optional | When to stop and ask a human |\n\n## Operating Workflow\n\n### 1. Frame the task\n- Restate the goal in one sentence in your own words. If you cannot, ask.\n- Identify the smallest input that still contains everything Claude needs.\n- Decide whether the task is a **read** (Mythos), an **act** (Fable), or **read → act** (both).\n\n### 2. Load the context\n- Preserve original structure — headers, file boundaries, page numbers.\n- Order matters: put stable, high-signal context first; volatile context last.\n- Use XML-style tags (`<file>`, `<doc>`, `<ticket>`) to make regions addressable.\n- Cache-friendly: keep the top of the prompt stable across calls to hit prompt-cache.\n\n### 3. Specify the output contract\n- Give the exact shape (markdown outline, JSON schema, or diff format).\n- State the null policy — what does Claude do when a field is unknown?\n- State the citation policy — is every claim traceable to an input region?\n\n### 4. Run with guardrails\n- Cap step count for Fable agents (typical: 8–25 steps).\n- Log every tool call, its arguments, its response, and its latency.\n- Route destructive ops (`rm`, `DROP`, `git push --force`) through an approval gate.\n\n### 5. Verify and iterate\n- Score output against the contract before returning to the user.\n- If invalid, re-ask with the validator error appended — do not silently accept.\n- Cache the successful prompt template — it is the reusable artefact.\n\n## Output Format\n\nThe skill produces:\n\n1. A **prompt template** (markdown or XML) ready to send to Claude 5.\n2. A **contract** describing the expected response shape.\n3. A **validator** (regex, JSON-schema, or unit-test snippet) that gates the response.\n4. A **runbook** describing how to re-run, refresh cache, and escalate.\n\nExample template head:\n\n```xml\n<system>\nYou are Claude 5 Long-Context operating under the \"long-context-1m-tokens\" skill.\nFollow the output contract exactly. If a required field is unknown, return\nnull with a `reason` explaining what is missing. Never fabricate citations.\n</system>\n\n<context>\n  <!-- structured, ordered, addressable -->\n</context>\n\n<task>\n  <goal>{goal}</goal>\n  <constraints>{constraints}</constraints>\n  <output_shape>{output_shape}</output_shape>\n</task>\n```\n\n## Decision Logic\n\n- If the input **fits in a single Mythos window** → load it whole, no RAG.\n- If the input **exceeds the window** → chunk with overlap, then run a reducer pass.\n- If the task requires **action** → route through Fable with a tool manifest.\n- If the task requires **verifiable claims** → force citations and reject un-cited claims in the validator.\n- If confidence is low → escalate rather than guess.\n\n## Anti-patterns\n\n- **Summarising the input first.** Claude 5 does that better than your preprocessor and you lose fidelity.\n- **Ambiguous tool schemas.** Vague names cost you retries — Claude will call the wrong tool.\n- **Unbounded agent loops.** Every Fable run must have a step cap and a wall-clock cap.\n- **Silent JSON re-parse.** If Claude returns invalid JSON, surface the error and re-ask — do not paper over it.\n- **Cache-hostile prompts.** Injecting a timestamp at the top of the prompt kills prompt-cache and doubles your bill.\n- **Mixing Mythos and Fable in one call.** Pick the right variant per step; do not ask Mythos to act or Fable to write a research memo.\n\n## Quality Bar (definition of done)\n\n- Output validates against the declared contract on the first pass ≥ 90% of the time.\n- Every factual claim in a research-mode output carries a citation.\n- Every tool call in an agent-mode output is logged with args, response, and duration.\n- The prompt template is checked into the repo and versioned like code.\n- A re-run with the same inputs produces a semantically equivalent output.\n\n## Worked Micro-Example\n\n**Goal.** 1M+ token windows let Claude Mythos reason across an entire repo, contract binder, or research corpus in a single pass. This skill teaches you the loading order, cache-friendly structure, and citation discipline that keeps recall high and hallucinations low.\n\n**Inputs.**\n- `context_source`: (task-specific — see argument-hint)\n- `goal`: single-sentence definition of done\n- `output_shape`: JSON matching the schema in `/schemas/long-context-1m-tokens.json`\n\n**Run.**\n1. Load context in the required order.\n2. Send the templated prompt to Claude 5 Long-Context.\n3. Validate the response against `/schemas/long-context-1m-tokens.json`.\n4. On failure, re-ask with the validator error; cap at 2 retries.\n5. Persist the successful response and the exact prompt used.\n\n**Expected artefact.** A machine-readable output plus a human-readable summary that a reviewer can approve in under 5 minutes.\n\n## Cadence\n\n| Cadence | Action |\n|---|---|\n| Per run | Emit trace: prompt hash, model id, tokens in/out, tool calls, validator result |\n| Per week | Review failure traces; tighten the schema, examples, or guardrails |\n| Per month | Rotate examples; retire ones that no longer surface real failure modes |\n| Per quarter | Re-benchmark against the current Claude 5 point release |\n\n## Companion skills\n\n- `prompt-engineering-playbook` — reusable prompt patterns across every model.\n- `mcp-agent-architecture` — deeper wiring for MCP-based tool use.\n- `rag-knowledge-systems` — when the corpus outgrows even a 1M window.\n- `executive-briefs` — for turning Claude 5 output into partner-ready one-pagers.\n\n## Rules (do not violate)\n\n- Never present Claude 5 output as human-authored for regulated deliverables.\n- Never let an agent execute a destructive command without a human gate.\n- Never disable the validator \"just to unblock a demo\" — that is how bad outputs ship.\n- Never hardcode secrets into the prompt; use tool-side injection.\n- Never remove the AI Content Notice — Claude 5 hallucinates and every output requires human review.\n\n## Closing note\n\nClaude 5 Mythos and Fable are powerful because they behave like a careful colleague, not a magic oracle. This skill is the frame that keeps the colleague honest, cheap, and reviewable.\n",
      "lines": 170,
      "tags": [
        "long-context",
        "claude-5",
        "mythos-fable"
      ],
      "summary": "1M+ token windows let Claude Mythos reason across an entire repo, contract binder, or research corpus in a single pass. This skill teaches you the loading order, cache-friendly structure, and citation discipline that keeps recall high and hallucinations low.",
      "audience": "AI engineers, agent builders, and platform teams using Claude 5",
      "whenToUse": "Use for tasks that require long-context reasoning with Claude 5 Mythos or Fable."
    },
    {
      "id": "13-claude-5/large-codebase-analysis",
      "name": "large-codebase-analysis",
      "displayName": "Whole-Codebase Analysis with Claude 5",
      "category": "Claude 5 Mythos & Fable",
      "categorySlug": "13-claude-5",
      "description": "Load a full monorepo into Claude 5 Mythos and produce dependency graphs, dead-code lists, architectural summaries, and safe refactor plans that respect cross-module contracts.",
      "argumentHint": "[repo path, languages, target artefact, exclude globs]",
      "allowedTools": "Read, Bash",
      "content": "---\nname: large-codebase-analysis\ndescription: Load a full monorepo into Claude 5 Mythos and produce dependency graphs, dead-code lists, architectural summaries, and safe refactor plans that respect cross-module contracts.\nargument-hint: [repo path, languages, target artefact, exclude globs]\nallowed-tools: Read, Bash\nmodel: claude-5-mythos | claude-5-fable\ncapability: Long-Context Reasoning\n---\n\n# Whole-Codebase Analysis with Claude 5\n\n> Claude 5 Mythos + Fable skill — Long-Context Reasoning. Optimised for Claude 5 Mythos (reasoning) and Claude 5 Fable (action / tool use), and backward-compatible with Claude Sonnet 4.5 and Opus 4.\n\n## TL;DR\n\nFeed Claude 5 the entire repo — not summaries — and get an architect-grade map: layered dependency graph, cyclic imports, hotspot files, dead exports, and a phased refactor plan sized for a single sprint.\n\n## When to use this skill\n\n- You are working with Claude 5 Mythos or Fable (or a downstream vendor exposing them).\n- The task benefits from **long-context reasoning** — do not use for tasks a smaller model handles cheaply.\n- You need a repeatable, reviewable prompt — not a one-off chat.\n- Output has to survive a code review, an audit, or a partner sign-off.\n\n## When NOT to use this skill\n\n- Simple lookups a smaller model (Haiku 4 / Sonnet 4.5) can serve at 10× lower cost.\n- Real-time UX where 1M-token loads would break the latency budget.\n- Tasks where a deterministic script would do — do not model what you can compute.\n\n## Inputs (fill these in)\n\n| Field | Required | Notes |\n|---|---|---|\n| `context_source` | ✅ | Repo path, doc set, ticket, screenshot, or MCP resource id |\n| `goal` | ✅ | The single sentence definition of done |\n| `audience` | ✅ | Reviewer, end-user, downstream agent, or auditor |\n| `constraints` | ✅ | Token budget, step budget, permitted tools, forbidden ops |\n| `output_shape` | ✅ | Free text / markdown / JSON schema / patch / PR |\n| `citations` | ⛔ optional | Required for research, legal, medical, financial outputs |\n| `escalation` | ⛔ optional | When to stop and ask a human |\n\n## Operating Workflow\n\n### 1. Frame the task\n- Restate the goal in one sentence in your own words. If you cannot, ask.\n- Identify the smallest input that still contains everything Claude needs.\n- Decide whether the task is a **read** (Mythos), an **act** (Fable), or **read → act** (both).\n\n### 2. Load the context\n- Preserve original structure — headers, file boundaries, page numbers.\n- Order matters: put stable, high-signal context first; volatile context last.\n- Use XML-style tags (`<file>`, `<doc>`, `<ticket>`) to make regions addressable.\n- Cache-friendly: keep the top of the prompt stable across calls to hit prompt-cache.\n\n### 3. Specify the output contract\n- Give the exact shape (markdown outline, JSON schema, or diff format).\n- State the null policy — what does Claude do when a field is unknown?\n- State the citation policy — is every claim traceable to an input region?\n\n### 4. Run with guardrails\n- Cap step count for Fable agents (typical: 8–25 steps).\n- Log every tool call, its arguments, its response, and its latency.\n- Route destructive ops (`rm`, `DROP`, `git push --force`) through an approval gate.\n\n### 5. Verify and iterate\n- Score output against the contract before returning to the user.\n- If invalid, re-ask with the validator error appended — do not silently accept.\n- Cache the successful prompt template — it is the reusable artefact.\n\n## Output Format\n\nThe skill produces:\n\n1. A **prompt template** (markdown or XML) ready to send to Claude 5.\n2. A **contract** describing the expected response shape.\n3. A **validator** (regex, JSON-schema, or unit-test snippet) that gates the response.\n4. A **runbook** describing how to re-run, refresh cache, and escalate.\n\nExample template head:\n\n```xml\n<system>\nYou are Claude 5 Long-Context operating under the \"large-codebase-analysis\" skill.\nFollow the output contract exactly. If a required field is unknown, return\nnull with a `reason` explaining what is missing. Never fabricate citations.\n</system>\n\n<context>\n  <!-- structured, ordered, addressable -->\n</context>\n\n<task>\n  <goal>{goal}</goal>\n  <constraints>{constraints}</constraints>\n  <output_shape>{output_shape}</output_shape>\n</task>\n```\n\n## Decision Logic\n\n- If the input **fits in a single Mythos window** → load it whole, no RAG.\n- If the input **exceeds the window** → chunk with overlap, then run a reducer pass.\n- If the task requires **action** → route through Fable with a tool manifest.\n- If the task requires **verifiable claims** → force citations and reject un-cited claims in the validator.\n- If confidence is low → escalate rather than guess.\n\n## Anti-patterns\n\n- **Summarising the input first.** Claude 5 does that better than your preprocessor and you lose fidelity.\n- **Ambiguous tool schemas.** Vague names cost you retries — Claude will call the wrong tool.\n- **Unbounded agent loops.** Every Fable run must have a step cap and a wall-clock cap.\n- **Silent JSON re-parse.** If Claude returns invalid JSON, surface the error and re-ask — do not paper over it.\n- **Cache-hostile prompts.** Injecting a timestamp at the top of the prompt kills prompt-cache and doubles your bill.\n- **Mixing Mythos and Fable in one call.** Pick the right variant per step; do not ask Mythos to act or Fable to write a research memo.\n\n## Quality Bar (definition of done)\n\n- Output validates against the declared contract on the first pass ≥ 90% of the time.\n- Every factual claim in a research-mode output carries a citation.\n- Every tool call in an agent-mode output is logged with args, response, and duration.\n- The prompt template is checked into the repo and versioned like code.\n- A re-run with the same inputs produces a semantically equivalent output.\n\n## Worked Micro-Example\n\n**Goal.** Feed Claude 5 the entire repo — not summaries — and get an architect-grade map: layered dependency graph, cyclic imports, hotspot files, dead exports, and a phased refactor plan sized for a single sprint.\n\n**Inputs.**\n- `context_source`: (task-specific — see argument-hint)\n- `goal`: single-sentence definition of done\n- `output_shape`: JSON matching the schema in `/schemas/large-codebase-analysis.json`\n\n**Run.**\n1. Load context in the required order.\n2. Send the templated prompt to Claude 5 Long-Context.\n3. Validate the response against `/schemas/large-codebase-analysis.json`.\n4. On failure, re-ask with the validator error; cap at 2 retries.\n5. Persist the successful response and the exact prompt used.\n\n**Expected artefact.** A machine-readable output plus a human-readable summary that a reviewer can approve in under 5 minutes.\n\n## Cadence\n\n| Cadence | Action |\n|---|---|\n| Per run | Emit trace: prompt hash, model id, tokens in/out, tool calls, validator result |\n| Per week | Review failure traces; tighten the schema, examples, or guardrails |\n| Per month | Rotate examples; retire ones that no longer surface real failure modes |\n| Per quarter | Re-benchmark against the current Claude 5 point release |\n\n## Companion skills\n\n- `prompt-engineering-playbook` — reusable prompt patterns across every model.\n- `mcp-agent-architecture` — deeper wiring for MCP-based tool use.\n- `rag-knowledge-systems` — when the corpus outgrows even a 1M window.\n- `executive-briefs` — for turning Claude 5 output into partner-ready one-pagers.\n\n## Rules (do not violate)\n\n- Never present Claude 5 output as human-authored for regulated deliverables.\n- Never let an agent execute a destructive command without a human gate.\n- Never disable the validator \"just to unblock a demo\" — that is how bad outputs ship.\n- Never hardcode secrets into the prompt; use tool-side injection.\n- Never remove the AI Content Notice — Claude 5 hallucinates and every output requires human review.\n\n## Closing note\n\nClaude 5 Mythos and Fable are powerful because they behave like a careful colleague, not a magic oracle. This skill is the frame that keeps the colleague honest, cheap, and reviewable.\n",
      "lines": 170,
      "tags": [
        "long-context",
        "claude-5",
        "mythos-fable"
      ],
      "summary": "Feed Claude 5 the entire repo — not summaries — and get an architect-grade map: layered dependency graph, cyclic imports, hotspot files, dead exports, and a phased refactor plan sized for a single sprint.",
      "audience": "AI engineers, agent builders, and platform teams using Claude 5",
      "whenToUse": "Use for tasks that require long-context reasoning with Claude 5 Mythos or Fable."
    },
    {
      "id": "13-claude-5/research-memo-synthesis",
      "name": "research-memo-synthesis",
      "displayName": "Multi-Document Research Memo Synthesis",
      "category": "Claude 5 Mythos & Fable",
      "categorySlug": "13-claude-5",
      "description": "Synthesise 50–500 sources into a single executive memo with numbered citations, contradictions surfaced, evidence tiers, and an audit trail that a partner can defend.",
      "argumentHint": "[topic, source list, audience, length target]",
      "allowedTools": "Read, Write",
      "content": "---\nname: research-memo-synthesis\ndescription: Synthesise 50–500 sources into a single executive memo with numbered citations, contradictions surfaced, evidence tiers, and an audit trail that a partner can defend.\nargument-hint: [topic, source list, audience, length target]\nallowed-tools: Read, Write\nmodel: claude-5-mythos | claude-5-fable\ncapability: Long-Context Reasoning\n---\n\n# Multi-Document Research Memo Synthesis\n\n> Claude 5 Mythos + Fable skill — Long-Context Reasoning. Optimised for Claude 5 Mythos (reasoning) and Claude 5 Fable (action / tool use), and backward-compatible with Claude Sonnet 4.5 and Opus 4.\n\n## TL;DR\n\nTurn a source pile into a defensible memo. Claude 5 loads every document, ranks evidence, quotes verbatim, flags contradictions, and produces a memo where every claim traces to a source id.\n\n## When to use this skill\n\n- You are working with Claude 5 Mythos or Fable (or a downstream vendor exposing them).\n- The task benefits from **long-context reasoning** — do not use for tasks a smaller model handles cheaply.\n- You need a repeatable, reviewable prompt — not a one-off chat.\n- Output has to survive a code review, an audit, or a partner sign-off.\n\n## When NOT to use this skill\n\n- Simple lookups a smaller model (Haiku 4 / Sonnet 4.5) can serve at 10× lower cost.\n- Real-time UX where 1M-token loads would break the latency budget.\n- Tasks where a deterministic script would do — do not model what you can compute.\n\n## Inputs (fill these in)\n\n| Field | Required | Notes |\n|---|---|---|\n| `context_source` | ✅ | Repo path, doc set, ticket, screenshot, or MCP resource id |\n| `goal` | ✅ | The single sentence definition of done |\n| `audience` | ✅ | Reviewer, end-user, downstream agent, or auditor |\n| `constraints` | ✅ | Token budget, step budget, permitted tools, forbidden ops |\n| `output_shape` | ✅ | Free text / markdown / JSON schema / patch / PR |\n| `citations` | ⛔ optional | Required for research, legal, medical, financial outputs |\n| `escalation` | ⛔ optional | When to stop and ask a human |\n\n## Operating Workflow\n\n### 1. Frame the task\n- Restate the goal in one sentence in your own words. If you cannot, ask.\n- Identify the smallest input that still contains everything Claude needs.\n- Decide whether the task is a **read** (Mythos), an **act** (Fable), or **read → act** (both).\n\n### 2. Load the context\n- Preserve original structure — headers, file boundaries, page numbers.\n- Order matters: put stable, high-signal context first; volatile context last.\n- Use XML-style tags (`<file>`, `<doc>`, `<ticket>`) to make regions addressable.\n- Cache-friendly: keep the top of the prompt stable across calls to hit prompt-cache.\n\n### 3. Specify the output contract\n- Give the exact shape (markdown outline, JSON schema, or diff format).\n- State the null policy — what does Claude do when a field is unknown?\n- State the citation policy — is every claim traceable to an input region?\n\n### 4. Run with guardrails\n- Cap step count for Fable agents (typical: 8–25 steps).\n- Log every tool call, its arguments, its response, and its latency.\n- Route destructive ops (`rm`, `DROP`, `git push --force`) through an approval gate.\n\n### 5. Verify and iterate\n- Score output against the contract before returning to the user.\n- If invalid, re-ask with the validator error appended — do not silently accept.\n- Cache the successful prompt template — it is the reusable artefact.\n\n## Output Format\n\nThe skill produces:\n\n1. A **prompt template** (markdown or XML) ready to send to Claude 5.\n2. A **contract** describing the expected response shape.\n3. A **validator** (regex, JSON-schema, or unit-test snippet) that gates the response.\n4. A **runbook** describing how to re-run, refresh cache, and escalate.\n\nExample template head:\n\n```xml\n<system>\nYou are Claude 5 Long-Context operating under the \"research-memo-synthesis\" skill.\nFollow the output contract exactly. If a required field is unknown, return\nnull with a `reason` explaining what is missing. Never fabricate citations.\n</system>\n\n<context>\n  <!-- structured, ordered, addressable -->\n</context>\n\n<task>\n  <goal>{goal}</goal>\n  <constraints>{constraints}</constraints>\n  <output_shape>{output_shape}</output_shape>\n</task>\n```\n\n## Decision Logic\n\n- If the input **fits in a single Mythos window** → load it whole, no RAG.\n- If the input **exceeds the window** → chunk with overlap, then run a reducer pass.\n- If the task requires **action** → route through Fable with a tool manifest.\n- If the task requires **verifiable claims** → force citations and reject un-cited claims in the validator.\n- If confidence is low → escalate rather than guess.\n\n## Anti-patterns\n\n- **Summarising the input first.** Claude 5 does that better than your preprocessor and you lose fidelity.\n- **Ambiguous tool schemas.** Vague names cost you retries — Claude will call the wrong tool.\n- **Unbounded agent loops.** Every Fable run must have a step cap and a wall-clock cap.\n- **Silent JSON re-parse.** If Claude returns invalid JSON, surface the error and re-ask — do not paper over it.\n- **Cache-hostile prompts.** Injecting a timestamp at the top of the prompt kills prompt-cache and doubles your bill.\n- **Mixing Mythos and Fable in one call.** Pick the right variant per step; do not ask Mythos to act or Fable to write a research memo.\n\n## Quality Bar (definition of done)\n\n- Output validates against the declared contract on the first pass ≥ 90% of the time.\n- Every factual claim in a research-mode output carries a citation.\n- Every tool call in an agent-mode output is logged with args, response, and duration.\n- The prompt template is checked into the repo and versioned like code.\n- A re-run with the same inputs produces a semantically equivalent output.\n\n## Worked Micro-Example\n\n**Goal.** Turn a source pile into a defensible memo. Claude 5 loads every document, ranks evidence, quotes verbatim, flags contradictions, and produces a memo where every claim traces to a source id.\n\n**Inputs.**\n- `context_source`: (task-specific — see argument-hint)\n- `goal`: single-sentence definition of done\n- `output_shape`: JSON matching the schema in `/schemas/research-memo-synthesis.json`\n\n**Run.**\n1. Load context in the required order.\n2. Send the templated prompt to Claude 5 Long-Context.\n3. Validate the response against `/schemas/research-memo-synthesis.json`.\n4. On failure, re-ask with the validator error; cap at 2 retries.\n5. Persist the successful response and the exact prompt used.\n\n**Expected artefact.** A machine-readable output plus a human-readable summary that a reviewer can approve in under 5 minutes.\n\n## Cadence\n\n| Cadence | Action |\n|---|---|\n| Per run | Emit trace: prompt hash, model id, tokens in/out, tool calls, validator result |\n| Per week | Review failure traces; tighten the schema, examples, or guardrails |\n| Per month | Rotate examples; retire ones that no longer surface real failure modes |\n| Per quarter | Re-benchmark against the current Claude 5 point release |\n\n## Companion skills\n\n- `prompt-engineering-playbook` — reusable prompt patterns across every model.\n- `mcp-agent-architecture` — deeper wiring for MCP-based tool use.\n- `rag-knowledge-systems` — when the corpus outgrows even a 1M window.\n- `executive-briefs` — for turning Claude 5 output into partner-ready one-pagers.\n\n## Rules (do not violate)\n\n- Never present Claude 5 output as human-authored for regulated deliverables.\n- Never let an agent execute a destructive command without a human gate.\n- Never disable the validator \"just to unblock a demo\" — that is how bad outputs ship.\n- Never hardcode secrets into the prompt; use tool-side injection.\n- Never remove the AI Content Notice — Claude 5 hallucinates and every output requires human review.\n\n## Closing note\n\nClaude 5 Mythos and Fable are powerful because they behave like a careful colleague, not a magic oracle. This skill is the frame that keeps the colleague honest, cheap, and reviewable.\n",
      "lines": 170,
      "tags": [
        "long-context",
        "claude-5",
        "mythos-fable"
      ],
      "summary": "Turn a source pile into a defensible memo. Claude 5 loads every document, ranks evidence, quotes verbatim, flags contradictions, and produces a memo where every claim traces to a source id.",
      "audience": "AI engineers, agent builders, and platform teams using Claude 5",
      "whenToUse": "Use for tasks that require long-context reasoning with Claude 5 Mythos or Fable."
    },
    {
      "id": "13-claude-5/agentic-workflow-design",
      "name": "agentic-workflow-design",
      "displayName": "Agentic Workflow Design (Claude 5 Fable)",
      "category": "Claude 5 Mythos & Fable",
      "categorySlug": "13-claude-5",
      "description": "Design multi-step Claude 5 Fable agents that plan, act, observe, and correct — with explicit budget, checkpoint, and human-in-the-loop gates you can defend in production.",
      "argumentHint": "[goal, tools available, budget, autonomy level]",
      "allowedTools": "Read, Write, Bash",
      "content": "---\nname: agentic-workflow-design\ndescription: Design multi-step Claude 5 Fable agents that plan, act, observe, and correct — with explicit budget, checkpoint, and human-in-the-loop gates you can defend in production.\nargument-hint: [goal, tools available, budget, autonomy level]\nallowed-tools: Read, Write, Bash\nmodel: claude-5-mythos | claude-5-fable\ncapability: Agentic Workflows\n---\n\n# Agentic Workflow Design (Claude 5 Fable)\n\n> Claude 5 Mythos + Fable skill — Agentic Workflows. Optimised for Claude 5 Mythos (reasoning) and Claude 5 Fable (action / tool use), and backward-compatible with Claude Sonnet 4.5 and Opus 4.\n\n## TL;DR\n\nFable is Anthropic's action-tuned Claude 5 variant. This skill shows how to structure the plan/act/observe loop, cap step counts, place approval gates, and record traces so nothing runs unchecked.\n\n## When to use this skill\n\n- You are working with Claude 5 Mythos or Fable (or a downstream vendor exposing them).\n- The task benefits from **agentic workflows** — do not use for tasks a smaller model handles cheaply.\n- You need a repeatable, reviewable prompt — not a one-off chat.\n- Output has to survive a code review, an audit, or a partner sign-off.\n\n## When NOT to use this skill\n\n- Simple lookups a smaller model (Haiku 4 / Sonnet 4.5) can serve at 10× lower cost.\n- Real-time UX where 1M-token loads would break the latency budget.\n- Tasks where a deterministic script would do — do not model what you can compute.\n\n## Inputs (fill these in)\n\n| Field | Required | Notes |\n|---|---|---|\n| `context_source` | ✅ | Repo path, doc set, ticket, screenshot, or MCP resource id |\n| `goal` | ✅ | The single sentence definition of done |\n| `audience` | ✅ | Reviewer, end-user, downstream agent, or auditor |\n| `constraints` | ✅ | Token budget, step budget, permitted tools, forbidden ops |\n| `output_shape` | ✅ | Free text / markdown / JSON schema / patch / PR |\n| `citations` | ⛔ optional | Required for research, legal, medical, financial outputs |\n| `escalation` | ⛔ optional | When to stop and ask a human |\n\n## Operating Workflow\n\n### 1. Frame the task\n- Restate the goal in one sentence in your own words. If you cannot, ask.\n- Identify the smallest input that still contains everything Claude needs.\n- Decide whether the task is a **read** (Mythos), an **act** (Fable), or **read → act** (both).\n\n### 2. Load the context\n- Preserve original structure — headers, file boundaries, page numbers.\n- Order matters: put stable, high-signal context first; volatile context last.\n- Use XML-style tags (`<file>`, `<doc>`, `<ticket>`) to make regions addressable.\n- Cache-friendly: keep the top of the prompt stable across calls to hit prompt-cache.\n\n### 3. Specify the output contract\n- Give the exact shape (markdown outline, JSON schema, or diff format).\n- State the null policy — what does Claude do when a field is unknown?\n- State the citation policy — is every claim traceable to an input region?\n\n### 4. Run with guardrails\n- Cap step count for Fable agents (typical: 8–25 steps).\n- Log every tool call, its arguments, its response, and its latency.\n- Route destructive ops (`rm`, `DROP`, `git push --force`) through an approval gate.\n\n### 5. Verify and iterate\n- Score output against the contract before returning to the user.\n- If invalid, re-ask with the validator error appended — do not silently accept.\n- Cache the successful prompt template — it is the reusable artefact.\n\n## Output Format\n\nThe skill produces:\n\n1. A **prompt template** (markdown or XML) ready to send to Claude 5.\n2. A **contract** describing the expected response shape.\n3. A **validator** (regex, JSON-schema, or unit-test snippet) that gates the response.\n4. A **runbook** describing how to re-run, refresh cache, and escalate.\n\nExample template head:\n\n```xml\n<system>\nYou are Claude 5 Agentic operating under the \"agentic-workflow-design\" skill.\nFollow the output contract exactly. If a required field is unknown, return\nnull with a `reason` explaining what is missing. Never fabricate citations.\n</system>\n\n<context>\n  <!-- structured, ordered, addressable -->\n</context>\n\n<task>\n  <goal>{goal}</goal>\n  <constraints>{constraints}</constraints>\n  <output_shape>{output_shape}</output_shape>\n</task>\n```\n\n## Decision Logic\n\n- If the input **fits in a single Mythos window** → load it whole, no RAG.\n- If the input **exceeds the window** → chunk with overlap, then run a reducer pass.\n- If the task requires **action** → route through Fable with a tool manifest.\n- If the task requires **verifiable claims** → force citations and reject un-cited claims in the validator.\n- If confidence is low → escalate rather than guess.\n\n## Anti-patterns\n\n- **Summarising the input first.** Claude 5 does that better than your preprocessor and you lose fidelity.\n- **Ambiguous tool schemas.** Vague names cost you retries — Claude will call the wrong tool.\n- **Unbounded agent loops.** Every Fable run must have a step cap and a wall-clock cap.\n- **Silent JSON re-parse.** If Claude returns invalid JSON, surface the error and re-ask — do not paper over it.\n- **Cache-hostile prompts.** Injecting a timestamp at the top of the prompt kills prompt-cache and doubles your bill.\n- **Mixing Mythos and Fable in one call.** Pick the right variant per step; do not ask Mythos to act or Fable to write a research memo.\n\n## Quality Bar (definition of done)\n\n- Output validates against the declared contract on the first pass ≥ 90% of the time.\n- Every factual claim in a research-mode output carries a citation.\n- Every tool call in an agent-mode output is logged with args, response, and duration.\n- The prompt template is checked into the repo and versioned like code.\n- A re-run with the same inputs produces a semantically equivalent output.\n\n## Worked Micro-Example\n\n**Goal.** Fable is Anthropic's action-tuned Claude 5 variant. This skill shows how to structure the plan/act/observe loop, cap step counts, place approval gates, and record traces so nothing runs unchecked.\n\n**Inputs.**\n- `context_source`: (task-specific — see argument-hint)\n- `goal`: single-sentence definition of done\n- `output_shape`: JSON matching the schema in `/schemas/agentic-workflow-design.json`\n\n**Run.**\n1. Load context in the required order.\n2. Send the templated prompt to Claude 5 Agentic.\n3. Validate the response against `/schemas/agentic-workflow-design.json`.\n4. On failure, re-ask with the validator error; cap at 2 retries.\n5. Persist the successful response and the exact prompt used.\n\n**Expected artefact.** A machine-readable output plus a human-readable summary that a reviewer can approve in under 5 minutes.\n\n## Cadence\n\n| Cadence | Action |\n|---|---|\n| Per run | Emit trace: prompt hash, model id, tokens in/out, tool calls, validator result |\n| Per week | Review failure traces; tighten the schema, examples, or guardrails |\n| Per month | Rotate examples; retire ones that no longer surface real failure modes |\n| Per quarter | Re-benchmark against the current Claude 5 point release |\n\n## Companion skills\n\n- `prompt-engineering-playbook` — reusable prompt patterns across every model.\n- `mcp-agent-architecture` — deeper wiring for MCP-based tool use.\n- `rag-knowledge-systems` — when the corpus outgrows even a 1M window.\n- `executive-briefs` — for turning Claude 5 output into partner-ready one-pagers.\n\n## Rules (do not violate)\n\n- Never present Claude 5 output as human-authored for regulated deliverables.\n- Never let an agent execute a destructive command without a human gate.\n- Never disable the validator \"just to unblock a demo\" — that is how bad outputs ship.\n- Never hardcode secrets into the prompt; use tool-side injection.\n- Never remove the AI Content Notice — Claude 5 hallucinates and every output requires human review.\n\n## Closing note\n\nClaude 5 Mythos and Fable are powerful because they behave like a careful colleague, not a magic oracle. This skill is the frame that keeps the colleague honest, cheap, and reviewable.\n",
      "lines": 170,
      "tags": [
        "agentic",
        "claude-5",
        "mythos-fable"
      ],
      "summary": "Fable is Anthropic's action-tuned Claude 5 variant. This skill shows how to structure the plan/act/observe loop, cap step counts, place approval gates, and record traces so nothing runs unchecked.",
      "audience": "AI engineers, agent builders, and platform teams using Claude 5",
      "whenToUse": "Use for tasks that require agentic workflows with Claude 5 Mythos or Fable."
    },
    {
      "id": "13-claude-5/mcp-tool-orchestration",
      "name": "mcp-tool-orchestration",
      "displayName": "MCP Tool Orchestration for Claude 5",
      "category": "Claude 5 Mythos & Fable",
      "categorySlug": "13-claude-5",
      "description": "Wire Claude 5 to MCP servers (filesystem, git, DB, Jira, Notion) with typed tool contracts, per-tool guardrails, error taxonomies, and traceable retries.",
      "argumentHint": "[MCP servers, environments, auth model, allowed operations]",
      "allowedTools": "Read, Write, Bash",
      "content": "---\nname: mcp-tool-orchestration\ndescription: Wire Claude 5 to MCP servers (filesystem, git, DB, Jira, Notion) with typed tool contracts, per-tool guardrails, error taxonomies, and traceable retries.\nargument-hint: [MCP servers, environments, auth model, allowed operations]\nallowed-tools: Read, Write, Bash\nmodel: claude-5-mythos | claude-5-fable\ncapability: Agentic Workflows\n---\n\n# MCP Tool Orchestration for Claude 5\n\n> Claude 5 Mythos + Fable skill — Agentic Workflows. Optimised for Claude 5 Mythos (reasoning) and Claude 5 Fable (action / tool use), and backward-compatible with Claude Sonnet 4.5 and Opus 4.\n\n## TL;DR\n\nMCP turns Claude into a real operator. This skill defines the tool manifest, guardrail matrix, retry policy, and audit log format that keep tool calls safe, cheap, and reproducible.\n\n## When to use this skill\n\n- You are working with Claude 5 Mythos or Fable (or a downstream vendor exposing them).\n- The task benefits from **agentic workflows** — do not use for tasks a smaller model handles cheaply.\n- You need a repeatable, reviewable prompt — not a one-off chat.\n- Output has to survive a code review, an audit, or a partner sign-off.\n\n## When NOT to use this skill\n\n- Simple lookups a smaller model (Haiku 4 / Sonnet 4.5) can serve at 10× lower cost.\n- Real-time UX where 1M-token loads would break the latency budget.\n- Tasks where a deterministic script would do — do not model what you can compute.\n\n## Inputs (fill these in)\n\n| Field | Required | Notes |\n|---|---|---|\n| `context_source` | ✅ | Repo path, doc set, ticket, screenshot, or MCP resource id |\n| `goal` | ✅ | The single sentence definition of done |\n| `audience` | ✅ | Reviewer, end-user, downstream agent, or auditor |\n| `constraints` | ✅ | Token budget, step budget, permitted tools, forbidden ops |\n| `output_shape` | ✅ | Free text / markdown / JSON schema / patch / PR |\n| `citations` | ⛔ optional | Required for research, legal, medical, financial outputs |\n| `escalation` | ⛔ optional | When to stop and ask a human |\n\n## Operating Workflow\n\n### 1. Frame the task\n- Restate the goal in one sentence in your own words. If you cannot, ask.\n- Identify the smallest input that still contains everything Claude needs.\n- Decide whether the task is a **read** (Mythos), an **act** (Fable), or **read → act** (both).\n\n### 2. Load the context\n- Preserve original structure — headers, file boundaries, page numbers.\n- Order matters: put stable, high-signal context first; volatile context last.\n- Use XML-style tags (`<file>`, `<doc>`, `<ticket>`) to make regions addressable.\n- Cache-friendly: keep the top of the prompt stable across calls to hit prompt-cache.\n\n### 3. Specify the output contract\n- Give the exact shape (markdown outline, JSON schema, or diff format).\n- State the null policy — what does Claude do when a field is unknown?\n- State the citation policy — is every claim traceable to an input region?\n\n### 4. Run with guardrails\n- Cap step count for Fable agents (typical: 8–25 steps).\n- Log every tool call, its arguments, its response, and its latency.\n- Route destructive ops (`rm`, `DROP`, `git push --force`) through an approval gate.\n\n### 5. Verify and iterate\n- Score output against the contract before returning to the user.\n- If invalid, re-ask with the validator error appended — do not silently accept.\n- Cache the successful prompt template — it is the reusable artefact.\n\n## Output Format\n\nThe skill produces:\n\n1. A **prompt template** (markdown or XML) ready to send to Claude 5.\n2. A **contract** describing the expected response shape.\n3. A **validator** (regex, JSON-schema, or unit-test snippet) that gates the response.\n4. A **runbook** describing how to re-run, refresh cache, and escalate.\n\nExample template head:\n\n```xml\n<system>\nYou are Claude 5 Agentic operating under the \"mcp-tool-orchestration\" skill.\nFollow the output contract exactly. If a required field is unknown, return\nnull with a `reason` explaining what is missing. Never fabricate citations.\n</system>\n\n<context>\n  <!-- structured, ordered, addressable -->\n</context>\n\n<task>\n  <goal>{goal}</goal>\n  <constraints>{constraints}</constraints>\n  <output_shape>{output_shape}</output_shape>\n</task>\n```\n\n## Decision Logic\n\n- If the input **fits in a single Mythos window** → load it whole, no RAG.\n- If the input **exceeds the window** → chunk with overlap, then run a reducer pass.\n- If the task requires **action** → route through Fable with a tool manifest.\n- If the task requires **verifiable claims** → force citations and reject un-cited claims in the validator.\n- If confidence is low → escalate rather than guess.\n\n## Anti-patterns\n\n- **Summarising the input first.** Claude 5 does that better than your preprocessor and you lose fidelity.\n- **Ambiguous tool schemas.** Vague names cost you retries — Claude will call the wrong tool.\n- **Unbounded agent loops.** Every Fable run must have a step cap and a wall-clock cap.\n- **Silent JSON re-parse.** If Claude returns invalid JSON, surface the error and re-ask — do not paper over it.\n- **Cache-hostile prompts.** Injecting a timestamp at the top of the prompt kills prompt-cache and doubles your bill.\n- **Mixing Mythos and Fable in one call.** Pick the right variant per step; do not ask Mythos to act or Fable to write a research memo.\n\n## Quality Bar (definition of done)\n\n- Output validates against the declared contract on the first pass ≥ 90% of the time.\n- Every factual claim in a research-mode output carries a citation.\n- Every tool call in an agent-mode output is logged with args, response, and duration.\n- The prompt template is checked into the repo and versioned like code.\n- A re-run with the same inputs produces a semantically equivalent output.\n\n## Worked Micro-Example\n\n**Goal.** MCP turns Claude into a real operator. This skill defines the tool manifest, guardrail matrix, retry policy, and audit log format that keep tool calls safe, cheap, and reproducible.\n\n**Inputs.**\n- `context_source`: (task-specific — see argument-hint)\n- `goal`: single-sentence definition of done\n- `output_shape`: JSON matching the schema in `/schemas/mcp-tool-orchestration.json`\n\n**Run.**\n1. Load context in the required order.\n2. Send the templated prompt to Claude 5 Agentic.\n3. Validate the response against `/schemas/mcp-tool-orchestration.json`.\n4. On failure, re-ask with the validator error; cap at 2 retries.\n5. Persist the successful response and the exact prompt used.\n\n**Expected artefact.** A machine-readable output plus a human-readable summary that a reviewer can approve in under 5 minutes.\n\n## Cadence\n\n| Cadence | Action |\n|---|---|\n| Per run | Emit trace: prompt hash, model id, tokens in/out, tool calls, validator result |\n| Per week | Review failure traces; tighten the schema, examples, or guardrails |\n| Per month | Rotate examples; retire ones that no longer surface real failure modes |\n| Per quarter | Re-benchmark against the current Claude 5 point release |\n\n## Companion skills\n\n- `prompt-engineering-playbook` — reusable prompt patterns across every model.\n- `mcp-agent-architecture` — deeper wiring for MCP-based tool use.\n- `rag-knowledge-systems` — when the corpus outgrows even a 1M window.\n- `executive-briefs` — for turning Claude 5 output into partner-ready one-pagers.\n\n## Rules (do not violate)\n\n- Never present Claude 5 output as human-authored for regulated deliverables.\n- Never let an agent execute a destructive command without a human gate.\n- Never disable the validator \"just to unblock a demo\" — that is how bad outputs ship.\n- Never hardcode secrets into the prompt; use tool-side injection.\n- Never remove the AI Content Notice — Claude 5 hallucinates and every output requires human review.\n\n## Closing note\n\nClaude 5 Mythos and Fable are powerful because they behave like a careful colleague, not a magic oracle. This skill is the frame that keeps the colleague honest, cheap, and reviewable.\n",
      "lines": 170,
      "tags": [
        "agentic",
        "claude-5",
        "mythos-fable"
      ],
      "summary": "MCP turns Claude into a real operator. This skill defines the tool manifest, guardrail matrix, retry policy, and audit log format that keep tool calls safe, cheap, and reproducible.",
      "audience": "AI engineers, agent builders, and platform teams using Claude 5",
      "whenToUse": "Use for tasks that require agentic workflows with Claude 5 Mythos or Fable."
    },
    {
      "id": "13-claude-5/autonomous-coding-agent",
      "name": "autonomous-coding-agent",
      "displayName": "Autonomous Coding Agent Blueprint",
      "category": "Claude 5 Mythos & Fable",
      "categorySlug": "13-claude-5",
      "description": "Blueprint for a Claude 5 Fable coding agent that reads a ticket, plans, edits, runs tests, opens a PR, and self-reviews — with guardrails against destructive ops.",
      "argumentHint": "[repo, ticket source, CI system, merge policy]",
      "allowedTools": "Read, Write, Bash",
      "content": "---\nname: autonomous-coding-agent\ndescription: Blueprint for a Claude 5 Fable coding agent that reads a ticket, plans, edits, runs tests, opens a PR, and self-reviews — with guardrails against destructive ops.\nargument-hint: [repo, ticket source, CI system, merge policy]\nallowed-tools: Read, Write, Bash\nmodel: claude-5-mythos | claude-5-fable\ncapability: Agentic Workflows\n---\n\n# Autonomous Coding Agent Blueprint\n\n> Claude 5 Mythos + Fable skill — Agentic Workflows. Optimised for Claude 5 Mythos (reasoning) and Claude 5 Fable (action / tool use), and backward-compatible with Claude Sonnet 4.5 and Opus 4.\n\n## TL;DR\n\nShip a coding agent that behaves like a careful junior: reads the ticket, writes a plan comment, edits in a branch, runs tests, iterates on failures, opens a PR with a self-review checklist, and stops on destructive commands.\n\n## When to use this skill\n\n- You are working with Claude 5 Mythos or Fable (or a downstream vendor exposing them).\n- The task benefits from **agentic workflows** — do not use for tasks a smaller model handles cheaply.\n- You need a repeatable, reviewable prompt — not a one-off chat.\n- Output has to survive a code review, an audit, or a partner sign-off.\n\n## When NOT to use this skill\n\n- Simple lookups a smaller model (Haiku 4 / Sonnet 4.5) can serve at 10× lower cost.\n- Real-time UX where 1M-token loads would break the latency budget.\n- Tasks where a deterministic script would do — do not model what you can compute.\n\n## Inputs (fill these in)\n\n| Field | Required | Notes |\n|---|---|---|\n| `context_source` | ✅ | Repo path, doc set, ticket, screenshot, or MCP resource id |\n| `goal` | ✅ | The single sentence definition of done |\n| `audience` | ✅ | Reviewer, end-user, downstream agent, or auditor |\n| `constraints` | ✅ | Token budget, step budget, permitted tools, forbidden ops |\n| `output_shape` | ✅ | Free text / markdown / JSON schema / patch / PR |\n| `citations` | ⛔ optional | Required for research, legal, medical, financial outputs |\n| `escalation` | ⛔ optional | When to stop and ask a human |\n\n## Operating Workflow\n\n### 1. Frame the task\n- Restate the goal in one sentence in your own words. If you cannot, ask.\n- Identify the smallest input that still contains everything Claude needs.\n- Decide whether the task is a **read** (Mythos), an **act** (Fable), or **read → act** (both).\n\n### 2. Load the context\n- Preserve original structure — headers, file boundaries, page numbers.\n- Order matters: put stable, high-signal context first; volatile context last.\n- Use XML-style tags (`<file>`, `<doc>`, `<ticket>`) to make regions addressable.\n- Cache-friendly: keep the top of the prompt stable across calls to hit prompt-cache.\n\n### 3. Specify the output contract\n- Give the exact shape (markdown outline, JSON schema, or diff format).\n- State the null policy — what does Claude do when a field is unknown?\n- State the citation policy — is every claim traceable to an input region?\n\n### 4. Run with guardrails\n- Cap step count for Fable agents (typical: 8–25 steps).\n- Log every tool call, its arguments, its response, and its latency.\n- Route destructive ops (`rm`, `DROP`, `git push --force`) through an approval gate.\n\n### 5. Verify and iterate\n- Score output against the contract before returning to the user.\n- If invalid, re-ask with the validator error appended — do not silently accept.\n- Cache the successful prompt template — it is the reusable artefact.\n\n## Output Format\n\nThe skill produces:\n\n1. A **prompt template** (markdown or XML) ready to send to Claude 5.\n2. A **contract** describing the expected response shape.\n3. A **validator** (regex, JSON-schema, or unit-test snippet) that gates the response.\n4. A **runbook** describing how to re-run, refresh cache, and escalate.\n\nExample template head:\n\n```xml\n<system>\nYou are Claude 5 Agentic operating under the \"autonomous-coding-agent\" skill.\nFollow the output contract exactly. If a required field is unknown, return\nnull with a `reason` explaining what is missing. Never fabricate citations.\n</system>\n\n<context>\n  <!-- structured, ordered, addressable -->\n</context>\n\n<task>\n  <goal>{goal}</goal>\n  <constraints>{constraints}</constraints>\n  <output_shape>{output_shape}</output_shape>\n</task>\n```\n\n## Decision Logic\n\n- If the input **fits in a single Mythos window** → load it whole, no RAG.\n- If the input **exceeds the window** → chunk with overlap, then run a reducer pass.\n- If the task requires **action** → route through Fable with a tool manifest.\n- If the task requires **verifiable claims** → force citations and reject un-cited claims in the validator.\n- If confidence is low → escalate rather than guess.\n\n## Anti-patterns\n\n- **Summarising the input first.** Claude 5 does that better than your preprocessor and you lose fidelity.\n- **Ambiguous tool schemas.** Vague names cost you retries — Claude will call the wrong tool.\n- **Unbounded agent loops.** Every Fable run must have a step cap and a wall-clock cap.\n- **Silent JSON re-parse.** If Claude returns invalid JSON, surface the error and re-ask — do not paper over it.\n- **Cache-hostile prompts.** Injecting a timestamp at the top of the prompt kills prompt-cache and doubles your bill.\n- **Mixing Mythos and Fable in one call.** Pick the right variant per step; do not ask Mythos to act or Fable to write a research memo.\n\n## Quality Bar (definition of done)\n\n- Output validates against the declared contract on the first pass ≥ 90% of the time.\n- Every factual claim in a research-mode output carries a citation.\n- Every tool call in an agent-mode output is logged with args, response, and duration.\n- The prompt template is checked into the repo and versioned like code.\n- A re-run with the same inputs produces a semantically equivalent output.\n\n## Worked Micro-Example\n\n**Goal.** Ship a coding agent that behaves like a careful junior: reads the ticket, writes a plan comment, edits in a branch, runs tests, iterates on failures, opens a PR with a self-review checklist, and stops on destructive commands.\n\n**Inputs.**\n- `context_source`: (task-specific — see argument-hint)\n- `goal`: single-sentence definition of done\n- `output_shape`: JSON matching the schema in `/schemas/autonomous-coding-agent.json`\n\n**Run.**\n1. Load context in the required order.\n2. Send the templated prompt to Claude 5 Agentic.\n3. Validate the response against `/schemas/autonomous-coding-agent.json`.\n4. On failure, re-ask with the validator error; cap at 2 retries.\n5. Persist the successful response and the exact prompt used.\n\n**Expected artefact.** A machine-readable output plus a human-readable summary that a reviewer can approve in under 5 minutes.\n\n## Cadence\n\n| Cadence | Action |\n|---|---|\n| Per run | Emit trace: prompt hash, model id, tokens in/out, tool calls, validator result |\n| Per week | Review failure traces; tighten the schema, examples, or guardrails |\n| Per month | Rotate examples; retire ones that no longer surface real failure modes |\n| Per quarter | Re-benchmark against the current Claude 5 point release |\n\n## Companion skills\n\n- `prompt-engineering-playbook` — reusable prompt patterns across every model.\n- `mcp-agent-architecture` — deeper wiring for MCP-based tool use.\n- `rag-knowledge-systems` — when the corpus outgrows even a 1M window.\n- `executive-briefs` — for turning Claude 5 output into partner-ready one-pagers.\n\n## Rules (do not violate)\n\n- Never present Claude 5 output as human-authored for regulated deliverables.\n- Never let an agent execute a destructive command without a human gate.\n- Never disable the validator \"just to unblock a demo\" — that is how bad outputs ship.\n- Never hardcode secrets into the prompt; use tool-side injection.\n- Never remove the AI Content Notice — Claude 5 hallucinates and every output requires human review.\n\n## Closing note\n\nClaude 5 Mythos and Fable are powerful because they behave like a careful colleague, not a magic oracle. This skill is the frame that keeps the colleague honest, cheap, and reviewable.\n",
      "lines": 170,
      "tags": [
        "agentic",
        "claude-5",
        "mythos-fable"
      ],
      "summary": "Ship a coding agent that behaves like a careful junior: reads the ticket, writes a plan comment, edits in a branch, runs tests, iterates on failures, opens a PR with a self-review checklist, and stops on destructive commands.",
      "audience": "AI engineers, agent builders, and platform teams using Claude 5",
      "whenToUse": "Use for tasks that require agentic workflows with Claude 5 Mythos or Fable."
    },
    {
      "id": "13-claude-5/computer-use-automation",
      "name": "computer-use-automation",
      "displayName": "Computer Use Automation with Claude 5",
      "category": "Claude 5 Mythos & Fable",
      "categorySlug": "13-claude-5",
      "description": "Use Claude 5 Fable's computer-use capability to automate desktop and browser workflows with screen-grounded reasoning, safe click policies, and rollback checkpoints.",
      "argumentHint": "[target apps, tasks, screen resolution, permissions]",
      "allowedTools": "Read, Bash",
      "content": "---\nname: computer-use-automation\ndescription: Use Claude 5 Fable's computer-use capability to automate desktop and browser workflows with screen-grounded reasoning, safe click policies, and rollback checkpoints.\nargument-hint: [target apps, tasks, screen resolution, permissions]\nallowed-tools: Read, Bash\nmodel: claude-5-mythos | claude-5-fable\ncapability: Agentic Workflows\n---\n\n# Computer Use Automation with Claude 5\n\n> Claude 5 Mythos + Fable skill — Agentic Workflows. Optimised for Claude 5 Mythos (reasoning) and Claude 5 Fable (action / tool use), and backward-compatible with Claude Sonnet 4.5 and Opus 4.\n\n## TL;DR\n\nComputer use lets Claude 5 drive a real desktop. This skill defines the safe-click policy, allow-list of applications, screenshot cadence, and rollback discipline that keep automations auditable.\n\n## When to use this skill\n\n- You are working with Claude 5 Mythos or Fable (or a downstream vendor exposing them).\n- The task benefits from **agentic workflows** — do not use for tasks a smaller model handles cheaply.\n- You need a repeatable, reviewable prompt — not a one-off chat.\n- Output has to survive a code review, an audit, or a partner sign-off.\n\n## When NOT to use this skill\n\n- Simple lookups a smaller model (Haiku 4 / Sonnet 4.5) can serve at 10× lower cost.\n- Real-time UX where 1M-token loads would break the latency budget.\n- Tasks where a deterministic script would do — do not model what you can compute.\n\n## Inputs (fill these in)\n\n| Field | Required | Notes |\n|---|---|---|\n| `context_source` | ✅ | Repo path, doc set, ticket, screenshot, or MCP resource id |\n| `goal` | ✅ | The single sentence definition of done |\n| `audience` | ✅ | Reviewer, end-user, downstream agent, or auditor |\n| `constraints` | ✅ | Token budget, step budget, permitted tools, forbidden ops |\n| `output_shape` | ✅ | Free text / markdown / JSON schema / patch / PR |\n| `citations` | ⛔ optional | Required for research, legal, medical, financial outputs |\n| `escalation` | ⛔ optional | When to stop and ask a human |\n\n## Operating Workflow\n\n### 1. Frame the task\n- Restate the goal in one sentence in your own words. If you cannot, ask.\n- Identify the smallest input that still contains everything Claude needs.\n- Decide whether the task is a **read** (Mythos), an **act** (Fable), or **read → act** (both).\n\n### 2. Load the context\n- Preserve original structure — headers, file boundaries, page numbers.\n- Order matters: put stable, high-signal context first; volatile context last.\n- Use XML-style tags (`<file>`, `<doc>`, `<ticket>`) to make regions addressable.\n- Cache-friendly: keep the top of the prompt stable across calls to hit prompt-cache.\n\n### 3. Specify the output contract\n- Give the exact shape (markdown outline, JSON schema, or diff format).\n- State the null policy — what does Claude do when a field is unknown?\n- State the citation policy — is every claim traceable to an input region?\n\n### 4. Run with guardrails\n- Cap step count for Fable agents (typical: 8–25 steps).\n- Log every tool call, its arguments, its response, and its latency.\n- Route destructive ops (`rm`, `DROP`, `git push --force`) through an approval gate.\n\n### 5. Verify and iterate\n- Score output against the contract before returning to the user.\n- If invalid, re-ask with the validator error appended — do not silently accept.\n- Cache the successful prompt template — it is the reusable artefact.\n\n## Output Format\n\nThe skill produces:\n\n1. A **prompt template** (markdown or XML) ready to send to Claude 5.\n2. A **contract** describing the expected response shape.\n3. A **validator** (regex, JSON-schema, or unit-test snippet) that gates the response.\n4. A **runbook** describing how to re-run, refresh cache, and escalate.\n\nExample template head:\n\n```xml\n<system>\nYou are Claude 5 Agentic operating under the \"computer-use-automation\" skill.\nFollow the output contract exactly. If a required field is unknown, return\nnull with a `reason` explaining what is missing. Never fabricate citations.\n</system>\n\n<context>\n  <!-- structured, ordered, addressable -->\n</context>\n\n<task>\n  <goal>{goal}</goal>\n  <constraints>{constraints}</constraints>\n  <output_shape>{output_shape}</output_shape>\n</task>\n```\n\n## Decision Logic\n\n- If the input **fits in a single Mythos window** → load it whole, no RAG.\n- If the input **exceeds the window** → chunk with overlap, then run a reducer pass.\n- If the task requires **action** → route through Fable with a tool manifest.\n- If the task requires **verifiable claims** → force citations and reject un-cited claims in the validator.\n- If confidence is low → escalate rather than guess.\n\n## Anti-patterns\n\n- **Summarising the input first.** Claude 5 does that better than your preprocessor and you lose fidelity.\n- **Ambiguous tool schemas.** Vague names cost you retries — Claude will call the wrong tool.\n- **Unbounded agent loops.** Every Fable run must have a step cap and a wall-clock cap.\n- **Silent JSON re-parse.** If Claude returns invalid JSON, surface the error and re-ask — do not paper over it.\n- **Cache-hostile prompts.** Injecting a timestamp at the top of the prompt kills prompt-cache and doubles your bill.\n- **Mixing Mythos and Fable in one call.** Pick the right variant per step; do not ask Mythos to act or Fable to write a research memo.\n\n## Quality Bar (definition of done)\n\n- Output validates against the declared contract on the first pass ≥ 90% of the time.\n- Every factual claim in a research-mode output carries a citation.\n- Every tool call in an agent-mode output is logged with args, response, and duration.\n- The prompt template is checked into the repo and versioned like code.\n- A re-run with the same inputs produces a semantically equivalent output.\n\n## Worked Micro-Example\n\n**Goal.** Computer use lets Claude 5 drive a real desktop. This skill defines the safe-click policy, allow-list of applications, screenshot cadence, and rollback discipline that keep automations auditable.\n\n**Inputs.**\n- `context_source`: (task-specific — see argument-hint)\n- `goal`: single-sentence definition of done\n- `output_shape`: JSON matching the schema in `/schemas/computer-use-automation.json`\n\n**Run.**\n1. Load context in the required order.\n2. Send the templated prompt to Claude 5 Agentic.\n3. Validate the response against `/schemas/computer-use-automation.json`.\n4. On failure, re-ask with the validator error; cap at 2 retries.\n5. Persist the successful response and the exact prompt used.\n\n**Expected artefact.** A machine-readable output plus a human-readable summary that a reviewer can approve in under 5 minutes.\n\n## Cadence\n\n| Cadence | Action |\n|---|---|\n| Per run | Emit trace: prompt hash, model id, tokens in/out, tool calls, validator result |\n| Per week | Review failure traces; tighten the schema, examples, or guardrails |\n| Per month | Rotate examples; retire ones that no longer surface real failure modes |\n| Per quarter | Re-benchmark against the current Claude 5 point release |\n\n## Companion skills\n\n- `prompt-engineering-playbook` — reusable prompt patterns across every model.\n- `mcp-agent-architecture` — deeper wiring for MCP-based tool use.\n- `rag-knowledge-systems` — when the corpus outgrows even a 1M window.\n- `executive-briefs` — for turning Claude 5 output into partner-ready one-pagers.\n\n## Rules (do not violate)\n\n- Never present Claude 5 output as human-authored for regulated deliverables.\n- Never let an agent execute a destructive command without a human gate.\n- Never disable the validator \"just to unblock a demo\" — that is how bad outputs ship.\n- Never hardcode secrets into the prompt; use tool-side injection.\n- Never remove the AI Content Notice — Claude 5 hallucinates and every output requires human review.\n\n## Closing note\n\nClaude 5 Mythos and Fable are powerful because they behave like a careful colleague, not a magic oracle. This skill is the frame that keeps the colleague honest, cheap, and reviewable.\n",
      "lines": 170,
      "tags": [
        "agentic",
        "claude-5",
        "mythos-fable"
      ],
      "summary": "Computer use lets Claude 5 drive a real desktop. This skill defines the safe-click policy, allow-list of applications, screenshot cadence, and rollback discipline that keep automations auditable.",
      "audience": "AI engineers, agent builders, and platform teams using Claude 5",
      "whenToUse": "Use for tasks that require agentic workflows with Claude 5 Mythos or Fable."
    },
    {
      "id": "13-claude-5/structured-json-extraction",
      "name": "structured-json-extraction",
      "displayName": "Structured JSON Extraction",
      "category": "Claude 5 Mythos & Fable",
      "categorySlug": "13-claude-5",
      "description": "Extract structured JSON from messy text (contracts, tickets, emails, PDFs) with schema-locked outputs, null-safe fields, and confidence scores.",
      "argumentHint": "[input type, target schema, null policy, batch size]",
      "allowedTools": "Read, Write",
      "content": "---\nname: structured-json-extraction\ndescription: Extract structured JSON from messy text (contracts, tickets, emails, PDFs) with schema-locked outputs, null-safe fields, and confidence scores.\nargument-hint: [input type, target schema, null policy, batch size]\nallowed-tools: Read, Write\nmodel: claude-5-mythos | claude-5-fable\ncapability: Structured Output\n---\n\n# Structured JSON Extraction\n\n> Claude 5 Mythos + Fable skill — Structured Output. Optimised for Claude 5 Mythos (reasoning) and Claude 5 Fable (action / tool use), and backward-compatible with Claude Sonnet 4.5 and Opus 4.\n\n## TL;DR\n\nTurn any unstructured input into schema-valid JSON in one call. Claude 5 supports strict JSON mode; this skill wraps it with a null policy, per-field confidence, and a re-ask pattern for invalid outputs.\n\n## When to use this skill\n\n- You are working with Claude 5 Mythos or Fable (or a downstream vendor exposing them).\n- The task benefits from **structured output** — do not use for tasks a smaller model handles cheaply.\n- You need a repeatable, reviewable prompt — not a one-off chat.\n- Output has to survive a code review, an audit, or a partner sign-off.\n\n## When NOT to use this skill\n\n- Simple lookups a smaller model (Haiku 4 / Sonnet 4.5) can serve at 10× lower cost.\n- Real-time UX where 1M-token loads would break the latency budget.\n- Tasks where a deterministic script would do — do not model what you can compute.\n\n## Inputs (fill these in)\n\n| Field | Required | Notes |\n|---|---|---|\n| `context_source` | ✅ | Repo path, doc set, ticket, screenshot, or MCP resource id |\n| `goal` | ✅ | The single sentence definition of done |\n| `audience` | ✅ | Reviewer, end-user, downstream agent, or auditor |\n| `constraints` | ✅ | Token budget, step budget, permitted tools, forbidden ops |\n| `output_shape` | ✅ | Free text / markdown / JSON schema / patch / PR |\n| `citations` | ⛔ optional | Required for research, legal, medical, financial outputs |\n| `escalation` | ⛔ optional | When to stop and ask a human |\n\n## Operating Workflow\n\n### 1. Frame the task\n- Restate the goal in one sentence in your own words. If you cannot, ask.\n- Identify the smallest input that still contains everything Claude needs.\n- Decide whether the task is a **read** (Mythos), an **act** (Fable), or **read → act** (both).\n\n### 2. Load the context\n- Preserve original structure — headers, file boundaries, page numbers.\n- Order matters: put stable, high-signal context first; volatile context last.\n- Use XML-style tags (`<file>`, `<doc>`, `<ticket>`) to make regions addressable.\n- Cache-friendly: keep the top of the prompt stable across calls to hit prompt-cache.\n\n### 3. Specify the output contract\n- Give the exact shape (markdown outline, JSON schema, or diff format).\n- State the null policy — what does Claude do when a field is unknown?\n- State the citation policy — is every claim traceable to an input region?\n\n### 4. Run with guardrails\n- Cap step count for Fable agents (typical: 8–25 steps).\n- Log every tool call, its arguments, its response, and its latency.\n- Route destructive ops (`rm`, `DROP`, `git push --force`) through an approval gate.\n\n### 5. Verify and iterate\n- Score output against the contract before returning to the user.\n- If invalid, re-ask with the validator error appended — do not silently accept.\n- Cache the successful prompt template — it is the reusable artefact.\n\n## Output Format\n\nThe skill produces:\n\n1. A **prompt template** (markdown or XML) ready to send to Claude 5.\n2. A **contract** describing the expected response shape.\n3. A **validator** (regex, JSON-schema, or unit-test snippet) that gates the response.\n4. A **runbook** describing how to re-run, refresh cache, and escalate.\n\nExample template head:\n\n```xml\n<system>\nYou are Claude 5 Structured operating under the \"structured-json-extraction\" skill.\nFollow the output contract exactly. If a required field is unknown, return\nnull with a `reason` explaining what is missing. Never fabricate citations.\n</system>\n\n<context>\n  <!-- structured, ordered, addressable -->\n</context>\n\n<task>\n  <goal>{goal}</goal>\n  <constraints>{constraints}</constraints>\n  <output_shape>{output_shape}</output_shape>\n</task>\n```\n\n## Decision Logic\n\n- If the input **fits in a single Mythos window** → load it whole, no RAG.\n- If the input **exceeds the window** → chunk with overlap, then run a reducer pass.\n- If the task requires **action** → route through Fable with a tool manifest.\n- If the task requires **verifiable claims** → force citations and reject un-cited claims in the validator.\n- If confidence is low → escalate rather than guess.\n\n## Anti-patterns\n\n- **Summarising the input first.** Claude 5 does that better than your preprocessor and you lose fidelity.\n- **Ambiguous tool schemas.** Vague names cost you retries — Claude will call the wrong tool.\n- **Unbounded agent loops.** Every Fable run must have a step cap and a wall-clock cap.\n- **Silent JSON re-parse.** If Claude returns invalid JSON, surface the error and re-ask — do not paper over it.\n- **Cache-hostile prompts.** Injecting a timestamp at the top of the prompt kills prompt-cache and doubles your bill.\n- **Mixing Mythos and Fable in one call.** Pick the right variant per step; do not ask Mythos to act or Fable to write a research memo.\n\n## Quality Bar (definition of done)\n\n- Output validates against the declared contract on the first pass ≥ 90% of the time.\n- Every factual claim in a research-mode output carries a citation.\n- Every tool call in an agent-mode output is logged with args, response, and duration.\n- The prompt template is checked into the repo and versioned like code.\n- A re-run with the same inputs produces a semantically equivalent output.\n\n## Worked Micro-Example\n\n**Goal.** Turn any unstructured input into schema-valid JSON in one call. Claude 5 supports strict JSON mode; this skill wraps it with a null policy, per-field confidence, and a re-ask pattern for invalid outputs.\n\n**Inputs.**\n- `context_source`: (task-specific — see argument-hint)\n- `goal`: single-sentence definition of done\n- `output_shape`: JSON matching the schema in `/schemas/structured-json-extraction.json`\n\n**Run.**\n1. Load context in the required order.\n2. Send the templated prompt to Claude 5 Structured.\n3. Validate the response against `/schemas/structured-json-extraction.json`.\n4. On failure, re-ask with the validator error; cap at 2 retries.\n5. Persist the successful response and the exact prompt used.\n\n**Expected artefact.** A machine-readable output plus a human-readable summary that a reviewer can approve in under 5 minutes.\n\n## Cadence\n\n| Cadence | Action |\n|---|---|\n| Per run | Emit trace: prompt hash, model id, tokens in/out, tool calls, validator result |\n| Per week | Review failure traces; tighten the schema, examples, or guardrails |\n| Per month | Rotate examples; retire ones that no longer surface real failure modes |\n| Per quarter | Re-benchmark against the current Claude 5 point release |\n\n## Companion skills\n\n- `prompt-engineering-playbook` — reusable prompt patterns across every model.\n- `mcp-agent-architecture` — deeper wiring for MCP-based tool use.\n- `rag-knowledge-systems` — when the corpus outgrows even a 1M window.\n- `executive-briefs` — for turning Claude 5 output into partner-ready one-pagers.\n\n## Rules (do not violate)\n\n- Never present Claude 5 output as human-authored for regulated deliverables.\n- Never let an agent execute a destructive command without a human gate.\n- Never disable the validator \"just to unblock a demo\" — that is how bad outputs ship.\n- Never hardcode secrets into the prompt; use tool-side injection.\n- Never remove the AI Content Notice — Claude 5 hallucinates and every output requires human review.\n\n## Closing note\n\nClaude 5 Mythos and Fable are powerful because they behave like a careful colleague, not a magic oracle. This skill is the frame that keeps the colleague honest, cheap, and reviewable.\n",
      "lines": 170,
      "tags": [
        "structured",
        "claude-5",
        "mythos-fable"
      ],
      "summary": "Turn any unstructured input into schema-valid JSON in one call. Claude 5 supports strict JSON mode; this skill wraps it with a null policy, per-field confidence, and a re-ask pattern for invalid outputs.",
      "audience": "AI engineers, agent builders, and platform teams using Claude 5",
      "whenToUse": "Use for tasks that require structured output with Claude 5 Mythos or Fable."
    },
    {
      "id": "13-claude-5/tool-calling-patterns",
      "name": "tool-calling-patterns",
      "displayName": "Tool-Calling Patterns for Claude 5",
      "category": "Claude 5 Mythos & Fable",
      "categorySlug": "13-claude-5",
      "description": "Design tool schemas Claude 5 calls correctly on the first try — with clear names, tight parameter types, examples, and error-recovery contracts.",
      "argumentHint": "[tools to expose, arg types, error modes, parallel policy]",
      "allowedTools": "Read, Write",
      "content": "---\nname: tool-calling-patterns\ndescription: Design tool schemas Claude 5 calls correctly on the first try — with clear names, tight parameter types, examples, and error-recovery contracts.\nargument-hint: [tools to expose, arg types, error modes, parallel policy]\nallowed-tools: Read, Write\nmodel: claude-5-mythos | claude-5-fable\ncapability: Structured Output\n---\n\n# Tool-Calling Patterns for Claude 5\n\n> Claude 5 Mythos + Fable skill — Structured Output. Optimised for Claude 5 Mythos (reasoning) and Claude 5 Fable (action / tool use), and backward-compatible with Claude Sonnet 4.5 and Opus 4.\n\n## TL;DR\n\nTool calls fail when the schema is vague. This skill gives the naming rules, parameter typing, few-shot examples, and error-return contract that make Claude 5 call tools right the first time — and recover cleanly when it does not.\n\n## When to use this skill\n\n- You are working with Claude 5 Mythos or Fable (or a downstream vendor exposing them).\n- The task benefits from **structured output** — do not use for tasks a smaller model handles cheaply.\n- You need a repeatable, reviewable prompt — not a one-off chat.\n- Output has to survive a code review, an audit, or a partner sign-off.\n\n## When NOT to use this skill\n\n- Simple lookups a smaller model (Haiku 4 / Sonnet 4.5) can serve at 10× lower cost.\n- Real-time UX where 1M-token loads would break the latency budget.\n- Tasks where a deterministic script would do — do not model what you can compute.\n\n## Inputs (fill these in)\n\n| Field | Required | Notes |\n|---|---|---|\n| `context_source` | ✅ | Repo path, doc set, ticket, screenshot, or MCP resource id |\n| `goal` | ✅ | The single sentence definition of done |\n| `audience` | ✅ | Reviewer, end-user, downstream agent, or auditor |\n| `constraints` | ✅ | Token budget, step budget, permitted tools, forbidden ops |\n| `output_shape` | ✅ | Free text / markdown / JSON schema / patch / PR |\n| `citations` | ⛔ optional | Required for research, legal, medical, financial outputs |\n| `escalation` | ⛔ optional | When to stop and ask a human |\n\n## Operating Workflow\n\n### 1. Frame the task\n- Restate the goal in one sentence in your own words. If you cannot, ask.\n- Identify the smallest input that still contains everything Claude needs.\n- Decide whether the task is a **read** (Mythos), an **act** (Fable), or **read → act** (both).\n\n### 2. Load the context\n- Preserve original structure — headers, file boundaries, page numbers.\n- Order matters: put stable, high-signal context first; volatile context last.\n- Use XML-style tags (`<file>`, `<doc>`, `<ticket>`) to make regions addressable.\n- Cache-friendly: keep the top of the prompt stable across calls to hit prompt-cache.\n\n### 3. Specify the output contract\n- Give the exact shape (markdown outline, JSON schema, or diff format).\n- State the null policy — what does Claude do when a field is unknown?\n- State the citation policy — is every claim traceable to an input region?\n\n### 4. Run with guardrails\n- Cap step count for Fable agents (typical: 8–25 steps).\n- Log every tool call, its arguments, its response, and its latency.\n- Route destructive ops (`rm`, `DROP`, `git push --force`) through an approval gate.\n\n### 5. Verify and iterate\n- Score output against the contract before returning to the user.\n- If invalid, re-ask with the validator error appended — do not silently accept.\n- Cache the successful prompt template — it is the reusable artefact.\n\n## Output Format\n\nThe skill produces:\n\n1. A **prompt template** (markdown or XML) ready to send to Claude 5.\n2. A **contract** describing the expected response shape.\n3. A **validator** (regex, JSON-schema, or unit-test snippet) that gates the response.\n4. A **runbook** describing how to re-run, refresh cache, and escalate.\n\nExample template head:\n\n```xml\n<system>\nYou are Claude 5 Structured operating under the \"tool-calling-patterns\" skill.\nFollow the output contract exactly. If a required field is unknown, return\nnull with a `reason` explaining what is missing. Never fabricate citations.\n</system>\n\n<context>\n  <!-- structured, ordered, addressable -->\n</context>\n\n<task>\n  <goal>{goal}</goal>\n  <constraints>{constraints}</constraints>\n  <output_shape>{output_shape}</output_shape>\n</task>\n```\n\n## Decision Logic\n\n- If the input **fits in a single Mythos window** → load it whole, no RAG.\n- If the input **exceeds the window** → chunk with overlap, then run a reducer pass.\n- If the task requires **action** → route through Fable with a tool manifest.\n- If the task requires **verifiable claims** → force citations and reject un-cited claims in the validator.\n- If confidence is low → escalate rather than guess.\n\n## Anti-patterns\n\n- **Summarising the input first.** Claude 5 does that better than your preprocessor and you lose fidelity.\n- **Ambiguous tool schemas.** Vague names cost you retries — Claude will call the wrong tool.\n- **Unbounded agent loops.** Every Fable run must have a step cap and a wall-clock cap.\n- **Silent JSON re-parse.** If Claude returns invalid JSON, surface the error and re-ask — do not paper over it.\n- **Cache-hostile prompts.** Injecting a timestamp at the top of the prompt kills prompt-cache and doubles your bill.\n- **Mixing Mythos and Fable in one call.** Pick the right variant per step; do not ask Mythos to act or Fable to write a research memo.\n\n## Quality Bar (definition of done)\n\n- Output validates against the declared contract on the first pass ≥ 90% of the time.\n- Every factual claim in a research-mode output carries a citation.\n- Every tool call in an agent-mode output is logged with args, response, and duration.\n- The prompt template is checked into the repo and versioned like code.\n- A re-run with the same inputs produces a semantically equivalent output.\n\n## Worked Micro-Example\n\n**Goal.** Tool calls fail when the schema is vague. This skill gives the naming rules, parameter typing, few-shot examples, and error-return contract that make Claude 5 call tools right the first time — and recover cleanly when it does not.\n\n**Inputs.**\n- `context_source`: (task-specific — see argument-hint)\n- `goal`: single-sentence definition of done\n- `output_shape`: JSON matching the schema in `/schemas/tool-calling-patterns.json`\n\n**Run.**\n1. Load context in the required order.\n2. Send the templated prompt to Claude 5 Structured.\n3. Validate the response against `/schemas/tool-calling-patterns.json`.\n4. On failure, re-ask with the validator error; cap at 2 retries.\n5. Persist the successful response and the exact prompt used.\n\n**Expected artefact.** A machine-readable output plus a human-readable summary that a reviewer can approve in under 5 minutes.\n\n## Cadence\n\n| Cadence | Action |\n|---|---|\n| Per run | Emit trace: prompt hash, model id, tokens in/out, tool calls, validator result |\n| Per week | Review failure traces; tighten the schema, examples, or guardrails |\n| Per month | Rotate examples; retire ones that no longer surface real failure modes |\n| Per quarter | Re-benchmark against the current Claude 5 point release |\n\n## Companion skills\n\n- `prompt-engineering-playbook` — reusable prompt patterns across every model.\n- `mcp-agent-architecture` — deeper wiring for MCP-based tool use.\n- `rag-knowledge-systems` — when the corpus outgrows even a 1M window.\n- `executive-briefs` — for turning Claude 5 output into partner-ready one-pagers.\n\n## Rules (do not violate)\n\n- Never present Claude 5 output as human-authored for regulated deliverables.\n- Never let an agent execute a destructive command without a human gate.\n- Never disable the validator \"just to unblock a demo\" — that is how bad outputs ship.\n- Never hardcode secrets into the prompt; use tool-side injection.\n- Never remove the AI Content Notice — Claude 5 hallucinates and every output requires human review.\n\n## Closing note\n\nClaude 5 Mythos and Fable are powerful because they behave like a careful colleague, not a magic oracle. This skill is the frame that keeps the colleague honest, cheap, and reviewable.\n",
      "lines": 170,
      "tags": [
        "structured",
        "claude-5",
        "mythos-fable"
      ],
      "summary": "Tool calls fail when the schema is vague. This skill gives the naming rules, parameter typing, few-shot examples, and error-return contract that make Claude 5 call tools right the first time — and recover cleanly when it does not.",
      "audience": "AI engineers, agent builders, and platform teams using Claude 5",
      "whenToUse": "Use for tasks that require structured output with Claude 5 Mythos or Fable."
    },
    {
      "id": "13-claude-5/schema-driven-generation",
      "name": "schema-driven-generation",
      "displayName": "Schema-Driven Content Generation",
      "category": "Claude 5 Mythos & Fable",
      "categorySlug": "13-claude-5",
      "description": "Generate long-form content (RFCs, PRDs, runbooks, playbooks) from a JSON schema so every section is present, ordered, and reviewable.",
      "argumentHint": "[document type, section schema, tone, length per section]",
      "allowedTools": "Read, Write",
      "content": "---\nname: schema-driven-generation\ndescription: Generate long-form content (RFCs, PRDs, runbooks, playbooks) from a JSON schema so every section is present, ordered, and reviewable.\nargument-hint: [document type, section schema, tone, length per section]\nallowed-tools: Read, Write\nmodel: claude-5-mythos | claude-5-fable\ncapability: Structured Output\n---\n\n# Schema-Driven Content Generation\n\n> Claude 5 Mythos + Fable skill — Structured Output. Optimised for Claude 5 Mythos (reasoning) and Claude 5 Fable (action / tool use), and backward-compatible with Claude Sonnet 4.5 and Opus 4.\n\n## TL;DR\n\nFree-form generation drifts; schema-driven generation does not. Give Claude 5 the section outline as a schema and the output is complete, ordered, and diff-friendly across regenerations.\n\n## When to use this skill\n\n- You are working with Claude 5 Mythos or Fable (or a downstream vendor exposing them).\n- The task benefits from **structured output** — do not use for tasks a smaller model handles cheaply.\n- You need a repeatable, reviewable prompt — not a one-off chat.\n- Output has to survive a code review, an audit, or a partner sign-off.\n\n## When NOT to use this skill\n\n- Simple lookups a smaller model (Haiku 4 / Sonnet 4.5) can serve at 10× lower cost.\n- Real-time UX where 1M-token loads would break the latency budget.\n- Tasks where a deterministic script would do — do not model what you can compute.\n\n## Inputs (fill these in)\n\n| Field | Required | Notes |\n|---|---|---|\n| `context_source` | ✅ | Repo path, doc set, ticket, screenshot, or MCP resource id |\n| `goal` | ✅ | The single sentence definition of done |\n| `audience` | ✅ | Reviewer, end-user, downstream agent, or auditor |\n| `constraints` | ✅ | Token budget, step budget, permitted tools, forbidden ops |\n| `output_shape` | ✅ | Free text / markdown / JSON schema / patch / PR |\n| `citations` | ⛔ optional | Required for research, legal, medical, financial outputs |\n| `escalation` | ⛔ optional | When to stop and ask a human |\n\n## Operating Workflow\n\n### 1. Frame the task\n- Restate the goal in one sentence in your own words. If you cannot, ask.\n- Identify the smallest input that still contains everything Claude needs.\n- Decide whether the task is a **read** (Mythos), an **act** (Fable), or **read → act** (both).\n\n### 2. Load the context\n- Preserve original structure — headers, file boundaries, page numbers.\n- Order matters: put stable, high-signal context first; volatile context last.\n- Use XML-style tags (`<file>`, `<doc>`, `<ticket>`) to make regions addressable.\n- Cache-friendly: keep the top of the prompt stable across calls to hit prompt-cache.\n\n### 3. Specify the output contract\n- Give the exact shape (markdown outline, JSON schema, or diff format).\n- State the null policy — what does Claude do when a field is unknown?\n- State the citation policy — is every claim traceable to an input region?\n\n### 4. Run with guardrails\n- Cap step count for Fable agents (typical: 8–25 steps).\n- Log every tool call, its arguments, its response, and its latency.\n- Route destructive ops (`rm`, `DROP`, `git push --force`) through an approval gate.\n\n### 5. Verify and iterate\n- Score output against the contract before returning to the user.\n- If invalid, re-ask with the validator error appended — do not silently accept.\n- Cache the successful prompt template — it is the reusable artefact.\n\n## Output Format\n\nThe skill produces:\n\n1. A **prompt template** (markdown or XML) ready to send to Claude 5.\n2. A **contract** describing the expected response shape.\n3. A **validator** (regex, JSON-schema, or unit-test snippet) that gates the response.\n4. A **runbook** describing how to re-run, refresh cache, and escalate.\n\nExample template head:\n\n```xml\n<system>\nYou are Claude 5 Structured operating under the \"schema-driven-generation\" skill.\nFollow the output contract exactly. If a required field is unknown, return\nnull with a `reason` explaining what is missing. Never fabricate citations.\n</system>\n\n<context>\n  <!-- structured, ordered, addressable -->\n</context>\n\n<task>\n  <goal>{goal}</goal>\n  <constraints>{constraints}</constraints>\n  <output_shape>{output_shape}</output_shape>\n</task>\n```\n\n## Decision Logic\n\n- If the input **fits in a single Mythos window** → load it whole, no RAG.\n- If the input **exceeds the window** → chunk with overlap, then run a reducer pass.\n- If the task requires **action** → route through Fable with a tool manifest.\n- If the task requires **verifiable claims** → force citations and reject un-cited claims in the validator.\n- If confidence is low → escalate rather than guess.\n\n## Anti-patterns\n\n- **Summarising the input first.** Claude 5 does that better than your preprocessor and you lose fidelity.\n- **Ambiguous tool schemas.** Vague names cost you retries — Claude will call the wrong tool.\n- **Unbounded agent loops.** Every Fable run must have a step cap and a wall-clock cap.\n- **Silent JSON re-parse.** If Claude returns invalid JSON, surface the error and re-ask — do not paper over it.\n- **Cache-hostile prompts.** Injecting a timestamp at the top of the prompt kills prompt-cache and doubles your bill.\n- **Mixing Mythos and Fable in one call.** Pick the right variant per step; do not ask Mythos to act or Fable to write a research memo.\n\n## Quality Bar (definition of done)\n\n- Output validates against the declared contract on the first pass ≥ 90% of the time.\n- Every factual claim in a research-mode output carries a citation.\n- Every tool call in an agent-mode output is logged with args, response, and duration.\n- The prompt template is checked into the repo and versioned like code.\n- A re-run with the same inputs produces a semantically equivalent output.\n\n## Worked Micro-Example\n\n**Goal.** Free-form generation drifts; schema-driven generation does not. Give Claude 5 the section outline as a schema and the output is complete, ordered, and diff-friendly across regenerations.\n\n**Inputs.**\n- `context_source`: (task-specific — see argument-hint)\n- `goal`: single-sentence definition of done\n- `output_shape`: JSON matching the schema in `/schemas/schema-driven-generation.json`\n\n**Run.**\n1. Load context in the required order.\n2. Send the templated prompt to Claude 5 Structured.\n3. Validate the response against `/schemas/schema-driven-generation.json`.\n4. On failure, re-ask with the validator error; cap at 2 retries.\n5. Persist the successful response and the exact prompt used.\n\n**Expected artefact.** A machine-readable output plus a human-readable summary that a reviewer can approve in under 5 minutes.\n\n## Cadence\n\n| Cadence | Action |\n|---|---|\n| Per run | Emit trace: prompt hash, model id, tokens in/out, tool calls, validator result |\n| Per week | Review failure traces; tighten the schema, examples, or guardrails |\n| Per month | Rotate examples; retire ones that no longer surface real failure modes |\n| Per quarter | Re-benchmark against the current Claude 5 point release |\n\n## Companion skills\n\n- `prompt-engineering-playbook` — reusable prompt patterns across every model.\n- `mcp-agent-architecture` — deeper wiring for MCP-based tool use.\n- `rag-knowledge-systems` — when the corpus outgrows even a 1M window.\n- `executive-briefs` — for turning Claude 5 output into partner-ready one-pagers.\n\n## Rules (do not violate)\n\n- Never present Claude 5 output as human-authored for regulated deliverables.\n- Never let an agent execute a destructive command without a human gate.\n- Never disable the validator \"just to unblock a demo\" — that is how bad outputs ship.\n- Never hardcode secrets into the prompt; use tool-side injection.\n- Never remove the AI Content Notice — Claude 5 hallucinates and every output requires human review.\n\n## Closing note\n\nClaude 5 Mythos and Fable are powerful because they behave like a careful colleague, not a magic oracle. This skill is the frame that keeps the colleague honest, cheap, and reviewable.\n",
      "lines": 170,
      "tags": [
        "structured",
        "claude-5",
        "mythos-fable"
      ],
      "summary": "Free-form generation drifts; schema-driven generation does not. Give Claude 5 the section outline as a schema and the output is complete, ordered, and diff-friendly across regenerations.",
      "audience": "AI engineers, agent builders, and platform teams using Claude 5",
      "whenToUse": "Use for tasks that require structured output with Claude 5 Mythos or Fable."
    },
    {
      "id": "13-claude-5/vision-screenshot-to-code",
      "name": "vision-screenshot-to-code",
      "displayName": "Screenshot → Production Code",
      "category": "Claude 5 Mythos & Fable",
      "categorySlug": "13-claude-5",
      "description": "Turn a UI screenshot or Figma export into production-ready React + Tailwind + shadcn code with accessibility, responsive breakpoints, and semantic tokens.",
      "argumentHint": "[screenshot, target framework, design system, breakpoints]",
      "allowedTools": "Read, Write",
      "content": "---\nname: vision-screenshot-to-code\ndescription: Turn a UI screenshot or Figma export into production-ready React + Tailwind + shadcn code with accessibility, responsive breakpoints, and semantic tokens.\nargument-hint: [screenshot, target framework, design system, breakpoints]\nallowed-tools: Read, Write\nmodel: claude-5-mythos | claude-5-fable\ncapability: Multimodal\n---\n\n# Screenshot → Production Code\n\n> Claude 5 Mythos + Fable skill — Multimodal. Optimised for Claude 5 Mythos (reasoning) and Claude 5 Fable (action / tool use), and backward-compatible with Claude Sonnet 4.5 and Opus 4.\n\n## TL;DR\n\nClaude 5 reads pixels. This skill turns a screenshot into semantic React + Tailwind that respects your design tokens, hits accessibility contrast, and ships responsive breakpoints instead of hardcoded widths.\n\n## When to use this skill\n\n- You are working with Claude 5 Mythos or Fable (or a downstream vendor exposing them).\n- The task benefits from **multimodal** — do not use for tasks a smaller model handles cheaply.\n- You need a repeatable, reviewable prompt — not a one-off chat.\n- Output has to survive a code review, an audit, or a partner sign-off.\n\n## When NOT to use this skill\n\n- Simple lookups a smaller model (Haiku 4 / Sonnet 4.5) can serve at 10× lower cost.\n- Real-time UX where 1M-token loads would break the latency budget.\n- Tasks where a deterministic script would do — do not model what you can compute.\n\n## Inputs (fill these in)\n\n| Field | Required | Notes |\n|---|---|---|\n| `context_source` | ✅ | Repo path, doc set, ticket, screenshot, or MCP resource id |\n| `goal` | ✅ | The single sentence definition of done |\n| `audience` | ✅ | Reviewer, end-user, downstream agent, or auditor |\n| `constraints` | ✅ | Token budget, step budget, permitted tools, forbidden ops |\n| `output_shape` | ✅ | Free text / markdown / JSON schema / patch / PR |\n| `citations` | ⛔ optional | Required for research, legal, medical, financial outputs |\n| `escalation` | ⛔ optional | When to stop and ask a human |\n\n## Operating Workflow\n\n### 1. Frame the task\n- Restate the goal in one sentence in your own words. If you cannot, ask.\n- Identify the smallest input that still contains everything Claude needs.\n- Decide whether the task is a **read** (Mythos), an **act** (Fable), or **read → act** (both).\n\n### 2. Load the context\n- Preserve original structure — headers, file boundaries, page numbers.\n- Order matters: put stable, high-signal context first; volatile context last.\n- Use XML-style tags (`<file>`, `<doc>`, `<ticket>`) to make regions addressable.\n- Cache-friendly: keep the top of the prompt stable across calls to hit prompt-cache.\n\n### 3. Specify the output contract\n- Give the exact shape (markdown outline, JSON schema, or diff format).\n- State the null policy — what does Claude do when a field is unknown?\n- State the citation policy — is every claim traceable to an input region?\n\n### 4. Run with guardrails\n- Cap step count for Fable agents (typical: 8–25 steps).\n- Log every tool call, its arguments, its response, and its latency.\n- Route destructive ops (`rm`, `DROP`, `git push --force`) through an approval gate.\n\n### 5. Verify and iterate\n- Score output against the contract before returning to the user.\n- If invalid, re-ask with the validator error appended — do not silently accept.\n- Cache the successful prompt template — it is the reusable artefact.\n\n## Output Format\n\nThe skill produces:\n\n1. A **prompt template** (markdown or XML) ready to send to Claude 5.\n2. A **contract** describing the expected response shape.\n3. A **validator** (regex, JSON-schema, or unit-test snippet) that gates the response.\n4. A **runbook** describing how to re-run, refresh cache, and escalate.\n\nExample template head:\n\n```xml\n<system>\nYou are Claude 5 Multimodal operating under the \"vision-screenshot-to-code\" skill.\nFollow the output contract exactly. If a required field is unknown, return\nnull with a `reason` explaining what is missing. Never fabricate citations.\n</system>\n\n<context>\n  <!-- structured, ordered, addressable -->\n</context>\n\n<task>\n  <goal>{goal}</goal>\n  <constraints>{constraints}</constraints>\n  <output_shape>{output_shape}</output_shape>\n</task>\n```\n\n## Decision Logic\n\n- If the input **fits in a single Mythos window** → load it whole, no RAG.\n- If the input **exceeds the window** → chunk with overlap, then run a reducer pass.\n- If the task requires **action** → route through Fable with a tool manifest.\n- If the task requires **verifiable claims** → force citations and reject un-cited claims in the validator.\n- If confidence is low → escalate rather than guess.\n\n## Anti-patterns\n\n- **Summarising the input first.** Claude 5 does that better than your preprocessor and you lose fidelity.\n- **Ambiguous tool schemas.** Vague names cost you retries — Claude will call the wrong tool.\n- **Unbounded agent loops.** Every Fable run must have a step cap and a wall-clock cap.\n- **Silent JSON re-parse.** If Claude returns invalid JSON, surface the error and re-ask — do not paper over it.\n- **Cache-hostile prompts.** Injecting a timestamp at the top of the prompt kills prompt-cache and doubles your bill.\n- **Mixing Mythos and Fable in one call.** Pick the right variant per step; do not ask Mythos to act or Fable to write a research memo.\n\n## Quality Bar (definition of done)\n\n- Output validates against the declared contract on the first pass ≥ 90% of the time.\n- Every factual claim in a research-mode output carries a citation.\n- Every tool call in an agent-mode output is logged with args, response, and duration.\n- The prompt template is checked into the repo and versioned like code.\n- A re-run with the same inputs produces a semantically equivalent output.\n\n## Worked Micro-Example\n\n**Goal.** Claude 5 reads pixels. This skill turns a screenshot into semantic React + Tailwind that respects your design tokens, hits accessibility contrast, and ships responsive breakpoints instead of hardcoded widths.\n\n**Inputs.**\n- `context_source`: (task-specific — see argument-hint)\n- `goal`: single-sentence definition of done\n- `output_shape`: JSON matching the schema in `/schemas/vision-screenshot-to-code.json`\n\n**Run.**\n1. Load context in the required order.\n2. Send the templated prompt to Claude 5 Multimodal.\n3. Validate the response against `/schemas/vision-screenshot-to-code.json`.\n4. On failure, re-ask with the validator error; cap at 2 retries.\n5. Persist the successful response and the exact prompt used.\n\n**Expected artefact.** A machine-readable output plus a human-readable summary that a reviewer can approve in under 5 minutes.\n\n## Cadence\n\n| Cadence | Action |\n|---|---|\n| Per run | Emit trace: prompt hash, model id, tokens in/out, tool calls, validator result |\n| Per week | Review failure traces; tighten the schema, examples, or guardrails |\n| Per month | Rotate examples; retire ones that no longer surface real failure modes |\n| Per quarter | Re-benchmark against the current Claude 5 point release |\n\n## Companion skills\n\n- `prompt-engineering-playbook` — reusable prompt patterns across every model.\n- `mcp-agent-architecture` — deeper wiring for MCP-based tool use.\n- `rag-knowledge-systems` — when the corpus outgrows even a 1M window.\n- `executive-briefs` — for turning Claude 5 output into partner-ready one-pagers.\n\n## Rules (do not violate)\n\n- Never present Claude 5 output as human-authored for regulated deliverables.\n- Never let an agent execute a destructive command without a human gate.\n- Never disable the validator \"just to unblock a demo\" — that is how bad outputs ship.\n- Never hardcode secrets into the prompt; use tool-side injection.\n- Never remove the AI Content Notice — Claude 5 hallucinates and every output requires human review.\n\n## Closing note\n\nClaude 5 Mythos and Fable are powerful because they behave like a careful colleague, not a magic oracle. This skill is the frame that keeps the colleague honest, cheap, and reviewable.\n",
      "lines": 170,
      "tags": [
        "multimodal",
        "claude-5",
        "mythos-fable"
      ],
      "summary": "Claude 5 reads pixels. This skill turns a screenshot into semantic React + Tailwind that respects your design tokens, hits accessibility contrast, and ships responsive breakpoints instead of hardcoded widths.",
      "audience": "AI engineers, agent builders, and platform teams using Claude 5",
      "whenToUse": "Use for tasks that require multimodal with Claude 5 Mythos or Fable."
    },
    {
      "id": "13-claude-5/pdf-diagram-reasoning",
      "name": "pdf-diagram-reasoning",
      "displayName": "PDF & Diagram Reasoning",
      "category": "Claude 5 Mythos & Fable",
      "categorySlug": "13-claude-5",
      "description": "Reason over PDFs, architecture diagrams, flowcharts, and whiteboard photos — extracting entities, relationships, and decisions with page-grounded citations.",
      "argumentHint": "[document set, diagram type, target extraction, citation format]",
      "allowedTools": "Read, Write",
      "content": "---\nname: pdf-diagram-reasoning\ndescription: Reason over PDFs, architecture diagrams, flowcharts, and whiteboard photos — extracting entities, relationships, and decisions with page-grounded citations.\nargument-hint: [document set, diagram type, target extraction, citation format]\nallowed-tools: Read, Write\nmodel: claude-5-mythos | claude-5-fable\ncapability: Multimodal\n---\n\n# PDF & Diagram Reasoning\n\n> Claude 5 Mythos + Fable skill — Multimodal. Optimised for Claude 5 Mythos (reasoning) and Claude 5 Fable (action / tool use), and backward-compatible with Claude Sonnet 4.5 and Opus 4.\n\n## TL;DR\n\nContracts, architecture diagrams, and whiteboard photos are first-class inputs for Claude 5. This skill sets the extraction schema and citation format so every extracted fact points back to a page and region.\n\n## When to use this skill\n\n- You are working with Claude 5 Mythos or Fable (or a downstream vendor exposing them).\n- The task benefits from **multimodal** — do not use for tasks a smaller model handles cheaply.\n- You need a repeatable, reviewable prompt — not a one-off chat.\n- Output has to survive a code review, an audit, or a partner sign-off.\n\n## When NOT to use this skill\n\n- Simple lookups a smaller model (Haiku 4 / Sonnet 4.5) can serve at 10× lower cost.\n- Real-time UX where 1M-token loads would break the latency budget.\n- Tasks where a deterministic script would do — do not model what you can compute.\n\n## Inputs (fill these in)\n\n| Field | Required | Notes |\n|---|---|---|\n| `context_source` | ✅ | Repo path, doc set, ticket, screenshot, or MCP resource id |\n| `goal` | ✅ | The single sentence definition of done |\n| `audience` | ✅ | Reviewer, end-user, downstream agent, or auditor |\n| `constraints` | ✅ | Token budget, step budget, permitted tools, forbidden ops |\n| `output_shape` | ✅ | Free text / markdown / JSON schema / patch / PR |\n| `citations` | ⛔ optional | Required for research, legal, medical, financial outputs |\n| `escalation` | ⛔ optional | When to stop and ask a human |\n\n## Operating Workflow\n\n### 1. Frame the task\n- Restate the goal in one sentence in your own words. If you cannot, ask.\n- Identify the smallest input that still contains everything Claude needs.\n- Decide whether the task is a **read** (Mythos), an **act** (Fable), or **read → act** (both).\n\n### 2. Load the context\n- Preserve original structure — headers, file boundaries, page numbers.\n- Order matters: put stable, high-signal context first; volatile context last.\n- Use XML-style tags (`<file>`, `<doc>`, `<ticket>`) to make regions addressable.\n- Cache-friendly: keep the top of the prompt stable across calls to hit prompt-cache.\n\n### 3. Specify the output contract\n- Give the exact shape (markdown outline, JSON schema, or diff format).\n- State the null policy — what does Claude do when a field is unknown?\n- State the citation policy — is every claim traceable to an input region?\n\n### 4. Run with guardrails\n- Cap step count for Fable agents (typical: 8–25 steps).\n- Log every tool call, its arguments, its response, and its latency.\n- Route destructive ops (`rm`, `DROP`, `git push --force`) through an approval gate.\n\n### 5. Verify and iterate\n- Score output against the contract before returning to the user.\n- If invalid, re-ask with the validator error appended — do not silently accept.\n- Cache the successful prompt template — it is the reusable artefact.\n\n## Output Format\n\nThe skill produces:\n\n1. A **prompt template** (markdown or XML) ready to send to Claude 5.\n2. A **contract** describing the expected response shape.\n3. A **validator** (regex, JSON-schema, or unit-test snippet) that gates the response.\n4. A **runbook** describing how to re-run, refresh cache, and escalate.\n\nExample template head:\n\n```xml\n<system>\nYou are Claude 5 Multimodal operating under the \"pdf-diagram-reasoning\" skill.\nFollow the output contract exactly. If a required field is unknown, return\nnull with a `reason` explaining what is missing. Never fabricate citations.\n</system>\n\n<context>\n  <!-- structured, ordered, addressable -->\n</context>\n\n<task>\n  <goal>{goal}</goal>\n  <constraints>{constraints}</constraints>\n  <output_shape>{output_shape}</output_shape>\n</task>\n```\n\n## Decision Logic\n\n- If the input **fits in a single Mythos window** → load it whole, no RAG.\n- If the input **exceeds the window** → chunk with overlap, then run a reducer pass.\n- If the task requires **action** → route through Fable with a tool manifest.\n- If the task requires **verifiable claims** → force citations and reject un-cited claims in the validator.\n- If confidence is low → escalate rather than guess.\n\n## Anti-patterns\n\n- **Summarising the input first.** Claude 5 does that better than your preprocessor and you lose fidelity.\n- **Ambiguous tool schemas.** Vague names cost you retries — Claude will call the wrong tool.\n- **Unbounded agent loops.** Every Fable run must have a step cap and a wall-clock cap.\n- **Silent JSON re-parse.** If Claude returns invalid JSON, surface the error and re-ask — do not paper over it.\n- **Cache-hostile prompts.** Injecting a timestamp at the top of the prompt kills prompt-cache and doubles your bill.\n- **Mixing Mythos and Fable in one call.** Pick the right variant per step; do not ask Mythos to act or Fable to write a research memo.\n\n## Quality Bar (definition of done)\n\n- Output validates against the declared contract on the first pass ≥ 90% of the time.\n- Every factual claim in a research-mode output carries a citation.\n- Every tool call in an agent-mode output is logged with args, response, and duration.\n- The prompt template is checked into the repo and versioned like code.\n- A re-run with the same inputs produces a semantically equivalent output.\n\n## Worked Micro-Example\n\n**Goal.** Contracts, architecture diagrams, and whiteboard photos are first-class inputs for Claude 5. This skill sets the extraction schema and citation format so every extracted fact points back to a page and region.\n\n**Inputs.**\n- `context_source`: (task-specific — see argument-hint)\n- `goal`: single-sentence definition of done\n- `output_shape`: JSON matching the schema in `/schemas/pdf-diagram-reasoning.json`\n\n**Run.**\n1. Load context in the required order.\n2. Send the templated prompt to Claude 5 Multimodal.\n3. Validate the response against `/schemas/pdf-diagram-reasoning.json`.\n4. On failure, re-ask with the validator error; cap at 2 retries.\n5. Persist the successful response and the exact prompt used.\n\n**Expected artefact.** A machine-readable output plus a human-readable summary that a reviewer can approve in under 5 minutes.\n\n## Cadence\n\n| Cadence | Action |\n|---|---|\n| Per run | Emit trace: prompt hash, model id, tokens in/out, tool calls, validator result |\n| Per week | Review failure traces; tighten the schema, examples, or guardrails |\n| Per month | Rotate examples; retire ones that no longer surface real failure modes |\n| Per quarter | Re-benchmark against the current Claude 5 point release |\n\n## Companion skills\n\n- `prompt-engineering-playbook` — reusable prompt patterns across every model.\n- `mcp-agent-architecture` — deeper wiring for MCP-based tool use.\n- `rag-knowledge-systems` — when the corpus outgrows even a 1M window.\n- `executive-briefs` — for turning Claude 5 output into partner-ready one-pagers.\n\n## Rules (do not violate)\n\n- Never present Claude 5 output as human-authored for regulated deliverables.\n- Never let an agent execute a destructive command without a human gate.\n- Never disable the validator \"just to unblock a demo\" — that is how bad outputs ship.\n- Never hardcode secrets into the prompt; use tool-side injection.\n- Never remove the AI Content Notice — Claude 5 hallucinates and every output requires human review.\n\n## Closing note\n\nClaude 5 Mythos and Fable are powerful because they behave like a careful colleague, not a magic oracle. This skill is the frame that keeps the colleague honest, cheap, and reviewable.\n",
      "lines": 170,
      "tags": [
        "multimodal",
        "claude-5",
        "mythos-fable"
      ],
      "summary": "Contracts, architecture diagrams, and whiteboard photos are first-class inputs for Claude 5. This skill sets the extraction schema and citation format so every extracted fact points back to a page and region.",
      "audience": "AI engineers, agent builders, and platform teams using Claude 5",
      "whenToUse": "Use for tasks that require multimodal with Claude 5 Mythos or Fable."
    },
    {
      "id": "13-claude-5/multimodal-doc-extraction",
      "name": "multimodal-doc-extraction",
      "displayName": "Multimodal Document Extraction",
      "category": "Claude 5 Mythos & Fable",
      "categorySlug": "13-claude-5",
      "description": "Extract structured data from mixed-media documents (scanned invoices, forms, ID cards, spreadsheets embedded in PDFs) with per-field confidence and human-review routing.",
      "argumentHint": "[document types, field schema, confidence threshold, review queue]",
      "allowedTools": "Read, Write",
      "content": "---\nname: multimodal-doc-extraction\ndescription: Extract structured data from mixed-media documents (scanned invoices, forms, ID cards, spreadsheets embedded in PDFs) with per-field confidence and human-review routing.\nargument-hint: [document types, field schema, confidence threshold, review queue]\nallowed-tools: Read, Write\nmodel: claude-5-mythos | claude-5-fable\ncapability: Multimodal\n---\n\n# Multimodal Document Extraction\n\n> Claude 5 Mythos + Fable skill — Multimodal. Optimised for Claude 5 Mythos (reasoning) and Claude 5 Fable (action / tool use), and backward-compatible with Claude Sonnet 4.5 and Opus 4.\n\n## TL;DR\n\nReal documents mix scans, forms, tables, and stamps. This skill uses Claude 5's multimodal reasoning to extract typed fields, score each field's confidence, and route low-confidence items to a human queue.\n\n## When to use this skill\n\n- You are working with Claude 5 Mythos or Fable (or a downstream vendor exposing them).\n- The task benefits from **multimodal** — do not use for tasks a smaller model handles cheaply.\n- You need a repeatable, reviewable prompt — not a one-off chat.\n- Output has to survive a code review, an audit, or a partner sign-off.\n\n## When NOT to use this skill\n\n- Simple lookups a smaller model (Haiku 4 / Sonnet 4.5) can serve at 10× lower cost.\n- Real-time UX where 1M-token loads would break the latency budget.\n- Tasks where a deterministic script would do — do not model what you can compute.\n\n## Inputs (fill these in)\n\n| Field | Required | Notes |\n|---|---|---|\n| `context_source` | ✅ | Repo path, doc set, ticket, screenshot, or MCP resource id |\n| `goal` | ✅ | The single sentence definition of done |\n| `audience` | ✅ | Reviewer, end-user, downstream agent, or auditor |\n| `constraints` | ✅ | Token budget, step budget, permitted tools, forbidden ops |\n| `output_shape` | ✅ | Free text / markdown / JSON schema / patch / PR |\n| `citations` | ⛔ optional | Required for research, legal, medical, financial outputs |\n| `escalation` | ⛔ optional | When to stop and ask a human |\n\n## Operating Workflow\n\n### 1. Frame the task\n- Restate the goal in one sentence in your own words. If you cannot, ask.\n- Identify the smallest input that still contains everything Claude needs.\n- Decide whether the task is a **read** (Mythos), an **act** (Fable), or **read → act** (both).\n\n### 2. Load the context\n- Preserve original structure — headers, file boundaries, page numbers.\n- Order matters: put stable, high-signal context first; volatile context last.\n- Use XML-style tags (`<file>`, `<doc>`, `<ticket>`) to make regions addressable.\n- Cache-friendly: keep the top of the prompt stable across calls to hit prompt-cache.\n\n### 3. Specify the output contract\n- Give the exact shape (markdown outline, JSON schema, or diff format).\n- State the null policy — what does Claude do when a field is unknown?\n- State the citation policy — is every claim traceable to an input region?\n\n### 4. Run with guardrails\n- Cap step count for Fable agents (typical: 8–25 steps).\n- Log every tool call, its arguments, its response, and its latency.\n- Route destructive ops (`rm`, `DROP`, `git push --force`) through an approval gate.\n\n### 5. Verify and iterate\n- Score output against the contract before returning to the user.\n- If invalid, re-ask with the validator error appended — do not silently accept.\n- Cache the successful prompt template — it is the reusable artefact.\n\n## Output Format\n\nThe skill produces:\n\n1. A **prompt template** (markdown or XML) ready to send to Claude 5.\n2. A **contract** describing the expected response shape.\n3. A **validator** (regex, JSON-schema, or unit-test snippet) that gates the response.\n4. A **runbook** describing how to re-run, refresh cache, and escalate.\n\nExample template head:\n\n```xml\n<system>\nYou are Claude 5 Multimodal operating under the \"multimodal-doc-extraction\" skill.\nFollow the output contract exactly. If a required field is unknown, return\nnull with a `reason` explaining what is missing. Never fabricate citations.\n</system>\n\n<context>\n  <!-- structured, ordered, addressable -->\n</context>\n\n<task>\n  <goal>{goal}</goal>\n  <constraints>{constraints}</constraints>\n  <output_shape>{output_shape}</output_shape>\n</task>\n```\n\n## Decision Logic\n\n- If the input **fits in a single Mythos window** → load it whole, no RAG.\n- If the input **exceeds the window** → chunk with overlap, then run a reducer pass.\n- If the task requires **action** → route through Fable with a tool manifest.\n- If the task requires **verifiable claims** → force citations and reject un-cited claims in the validator.\n- If confidence is low → escalate rather than guess.\n\n## Anti-patterns\n\n- **Summarising the input first.** Claude 5 does that better than your preprocessor and you lose fidelity.\n- **Ambiguous tool schemas.** Vague names cost you retries — Claude will call the wrong tool.\n- **Unbounded agent loops.** Every Fable run must have a step cap and a wall-clock cap.\n- **Silent JSON re-parse.** If Claude returns invalid JSON, surface the error and re-ask — do not paper over it.\n- **Cache-hostile prompts.** Injecting a timestamp at the top of the prompt kills prompt-cache and doubles your bill.\n- **Mixing Mythos and Fable in one call.** Pick the right variant per step; do not ask Mythos to act or Fable to write a research memo.\n\n## Quality Bar (definition of done)\n\n- Output validates against the declared contract on the first pass ≥ 90% of the time.\n- Every factual claim in a research-mode output carries a citation.\n- Every tool call in an agent-mode output is logged with args, response, and duration.\n- The prompt template is checked into the repo and versioned like code.\n- A re-run with the same inputs produces a semantically equivalent output.\n\n## Worked Micro-Example\n\n**Goal.** Real documents mix scans, forms, tables, and stamps. This skill uses Claude 5's multimodal reasoning to extract typed fields, score each field's confidence, and route low-confidence items to a human queue.\n\n**Inputs.**\n- `context_source`: (task-specific — see argument-hint)\n- `goal`: single-sentence definition of done\n- `output_shape`: JSON matching the schema in `/schemas/multimodal-doc-extraction.json`\n\n**Run.**\n1. Load context in the required order.\n2. Send the templated prompt to Claude 5 Multimodal.\n3. Validate the response against `/schemas/multimodal-doc-extraction.json`.\n4. On failure, re-ask with the validator error; cap at 2 retries.\n5. Persist the successful response and the exact prompt used.\n\n**Expected artefact.** A machine-readable output plus a human-readable summary that a reviewer can approve in under 5 minutes.\n\n## Cadence\n\n| Cadence | Action |\n|---|---|\n| Per run | Emit trace: prompt hash, model id, tokens in/out, tool calls, validator result |\n| Per week | Review failure traces; tighten the schema, examples, or guardrails |\n| Per month | Rotate examples; retire ones that no longer surface real failure modes |\n| Per quarter | Re-benchmark against the current Claude 5 point release |\n\n## Companion skills\n\n- `prompt-engineering-playbook` — reusable prompt patterns across every model.\n- `mcp-agent-architecture` — deeper wiring for MCP-based tool use.\n- `rag-knowledge-systems` — when the corpus outgrows even a 1M window.\n- `executive-briefs` — for turning Claude 5 output into partner-ready one-pagers.\n\n## Rules (do not violate)\n\n- Never present Claude 5 output as human-authored for regulated deliverables.\n- Never let an agent execute a destructive command without a human gate.\n- Never disable the validator \"just to unblock a demo\" — that is how bad outputs ship.\n- Never hardcode secrets into the prompt; use tool-side injection.\n- Never remove the AI Content Notice — Claude 5 hallucinates and every output requires human review.\n\n## Closing note\n\nClaude 5 Mythos and Fable are powerful because they behave like a careful colleague, not a magic oracle. This skill is the frame that keeps the colleague honest, cheap, and reviewable.\n",
      "lines": 170,
      "tags": [
        "multimodal",
        "claude-5",
        "mythos-fable"
      ],
      "summary": "Real documents mix scans, forms, tables, and stamps. This skill uses Claude 5's multimodal reasoning to extract typed fields, score each field's confidence, and route low-confidence items to a human queue.",
      "audience": "AI engineers, agent builders, and platform teams using Claude 5",
      "whenToUse": "Use for tasks that require multimodal with Claude 5 Mythos or Fable."
    }
  ]
}